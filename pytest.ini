[pytest]
markers =
    e2e: End-to-end tests that require a live database connection
    frontend_cross_repo: Tests whose subject includes files from the sibling frontend repo
    needs_local_data: Tests that need the live catalog/database or the local backend/.env (skipped in CI without NEON_DATABASE_URL)
