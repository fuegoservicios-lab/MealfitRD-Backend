__pycache__/
*.py[cod]
*$py.class
.env
.venv
venv/
ENV/
env.bak/
venv.bak/
test_venv/
venv-test/
.idea/
.vscode/
uploads/

# Logs y outputs transitorios
*.log
pytest_output.txt
title_debug.log
push_log.txt
pyright_results.json
*.bak
*.jsonl.bak

# Runtime state — generados por la app, no son source-of-truth
_force_regen.json
chat_errors.json
# Buffer local del cron _flush_pending_deferrals (CHUNK_DEFERRALS_BUFFER_PATH).
# Cada instancia mantiene su propio archivo; contiene UUIDs de usuarios — nunca
# se commitea. Default name viene de constants.py:CHUNK_DEFERRALS_BUFFER_PATH.
deferrals_pending.jsonl
# Análogo: buffer del cron _flush_pending_lesson_telemetry.
lesson_telemetry_pending.jsonl

# DBs y artefactos locales
*.sqlite3
*.db
test.mp3

# [I6 / P3-COVERAGE-HEATMAP · 2026-05-20] Output del coverage local.
# Regenerable via `./scripts/run_coverage.sh`.
htmlcov/
.coverage
.coverage.*
coverage.xml

# Sub-scratch / helpers one-shot dentro de backend
scratch/
# [P3-DEBUG-TIME-CLEANUP · 2026-05-20] `test_browser_console.js` removed
# from raíz; vive en scratch/legacy_root_helpers/ junto a refactor*.py.
# Entry omitida — scratch/ ya cubre.

# Helpers ad-hoc one-off (no deben commitearse al repo)
append_*.py
scratch_*.py
