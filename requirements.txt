fastapi==0.136.1
uvicorn==0.46.0
langchain==1.2.18
# [P0-GLM-MIGRATION · 2026-09-02] langchain-openai = cliente OpenAI-compatible que
# llm_provider.py apunta a https://api.z.ai/api/paas/v4 (glm-5.3-flash / glm-5.3).
langchain-openai==1.3.0
# [P1-COHERE-EMBED-V4 · 2026-06-12] SDK nativo para embeddings Cohere Embed v4
# (embed-v4.0 @1536, input_type asimétrico) — ver embeddings_provider.py.
cohere==7.0.4
# [P1-NEON-AUTH-MIGRATION · 2026-06-13] Supabase eliminado por completo.
# Auth → Neon Auth (Better Auth): el backend valida JWTs EdDSA contra el JWKS
# público (neon_auth.py). PyJWT + cryptography eran transitivos de supabase;
# ahora explícitos. Datos → Neon (psycopg). Storage de visual_diary queda
# pendiente de object storage (vision disabled).
PyJWT==2.10.1
cryptography==46.0.3
pydantic==2.13.4
python-dotenv==1.2.2
langgraph==1.1.10
langgraph-checkpoint-postgres==3.0.5
psycopg[binary,pool]==3.3.4
tenacity==9.1.4
python-multipart==0.0.28
redis==7.4.0
sentry-sdk[fastapi]==2.59.0
apscheduler==3.11.2
pywebpush==2.3.0
httpx==0.28.1
# [P1-VISION-LUNA · 2026-07-28] Redimensionado defensivo de fotos antes de
# enviarlas a un proveedor de visión (image_prep.py) — 6,6x más barato en
# tokens de entrada a 1024px vs foto cruda de móvil. Publica wheels
# manylinux para aarch64 (VPS Oracle ARM) desde hace varias major versions,
# así el deploy no compila desde fuente.
Pillow==12.2.0
# [BIOBOROS-3.0-HIGHS-SOLVER · 2026-09-07] Optimizador MILP/HiGHS para Bioboros 3.0.
# Publica wheels manylinux para aarch64 (VPS Oracle ARM) para instalación binaria directa.
# Fijada a 1.17.1 y no `>=`: scipy 1.18 dejo de publicar wheel cp311 para aarch64, asi que
# un `>=` instala 1.18 en un VPS con Python 3.12+ y en uno con 3.11 caeria a compilar desde
# fuente. La 1.17.1 tiene wheel ARM para cp311, cp312 y cp313 — vale sea cual sea el Python
# del VPS — y es la version contra la que corrio el gate en local.
scipy==1.17.1

