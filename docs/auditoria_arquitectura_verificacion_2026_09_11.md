# Verificación independiente de `BioBoros_auditoria_arquitectura.md` — 2026-09-11

**P-fix:** `P1-AUDITORIA-ARQ-VERIFICADA · 2026-09-11` · **Rama:** `claude/auditoria-arquitectura-2026-09-11` (backend, worktree aislado; sin merge ni deploy) · **Test:** `tests/test_p1_auditoria_arq_verificada.py`.

**Revisiones.** Auditada: backend `baf4163` (`P1-SODIO-DEL-DIA-DETERMINISTA`), frontend `be285b8`, workspace `bdc828d`. Verificado sobre: backend `866ac4d` (8 commits después), frontend `be285b8` (idéntico), workspace `81e3ecb` (sólo espejos de migraciones).

## 0. Lo que faltó: el informe y las 14 sondas

`BioBoros_auditoria_arquitectura.md` **no estaba en disco** (repo, Descargas, Escritorio, Documentos, perfil completo). Se trabajó con los 16 enunciados tal como los pasó el dueño y se contrastaron contra el código ejecutable, sus consumidores y la configuración viva del VPS. **Las 14 sondas no se han auditado**: no se inventó su contenido. Cuando llegue el archivo, cada sonda se puede contrastar contra la sección 3 (qué demuestra el código real) y contra los tests nuevos (qué comportamiento se espera ahora).

Dato que cambia la severidad de todo lo que toca al camino determinista: en producción `MEALFIT_DETERMINISTIC_DAY_USERS` tiene ids (canario del dueño) y `MEALFIT_RECIPE_LIBRARY_SELECT=1`. **El camino determinista está vivo para esos usuarios**; no es código latente.

## 1. Los 16 hallazgos

| # | Hallazgo | Veredicto | Evidencia (archivo:línea en 866ac4d) | Estado |
|---|---|---|---|---|
| 1 | `slots`/`protein` vs `meal_types`/`protein_pool` | **Confirmado y vigente** (más grave que el enunciado) | `deterministic_day.py:677` leía `skeleton_day["slots"]`, `:702` `skeleton_day["protein"]`; el único llamador (`graph_orchestrator.py:9289`) pasa un `DaySkeletonModel` (`schemas.py:69-88`) que trae `meal_types`/`protein_pool` y ninguna de las dos. Resultado: siempre 4 franjas de una tabla propia `_REPARTO` (.25/.35/.30/.10, distinta del SSOT `MEAL_SLOT_SPLITS` .20/.35/.15/.30) y `family=None`. Un plan clínico de 3 o 6 comidas recibía 4 (`_enforce_meal_count` sólo recorta). Ningún test lo vio: todos pasaban `{"slots": [...]}`. | **Corregido**: `_franjas_del_dia` (meal_types → franja registry vía `_SLOT_KEY_MAP`/`canonical_slot_es`, etiqueta intacta), `_fracciones_por_franja` (el mismo `_canonical_slot_fractions` del solver), `_familias_del_dia` (pool → familia, unión ordenada, fallback dicho). |
| 2 | Omisión de filtros, candidatos fijados y horizonte | **Parcialmente confirmado** | Aplicados: alergia, dieta, backstop, presupuesto. NO aplicados: CandidateSet fijado (consultaba el snapshot VIVO, `deterministic_day.py:702`, violando `docs/arq27_f3_candidateset.md`), `_blueprint_slice`, `market_country`, `require_known_nutrients`, `need_days/allow_frozen` (todos presentes en la llamada gemela de `horizon.py:511-515`), cocina sin `day_index` (`:661`). **Premisa falsa** en 4 docstrings: «no pasa por `assemble_plan_node`» — las aristas `graph_orchestrator.py:46905-46909` son incondicionales. | **Corregido**: `_candidatos` honra `registry.candidates["día:franja"]` ∩ filtros vivos; se pasan `market_country`, `required_nutrients(eff)`, `_dur_kwargs`; cocina con `day_index = days_offset + day_num − 1`; rotación por índice absoluto; docstrings corregidos; `_candidate_source` en cada plato. |
| 3 | Tiempo como texto libre y relleno «15 min» | **Confirmado y vigente** (camino LLM, todos los usuarios) | `graph_orchestrator.py:39476` `m["prep_time"] = "15 min"`; `prep_minutes_est` (`dish_registry.py:350`, P1-MINUTOS-DE-LA-RECETA) sin NINGÚN lector de producción; el camino determinista no emitía `prep_time`. `La bandera`: 105 min declarados, «15 min» servidos. | **Corregido**: `recipe_library.prep_time_for_meal/fill_prep_time` (receta/técnica; el `defecto` no se sirve); `assemble` rellena del registry o deja vacío + `_prep_time_source="unknown"` (el frontend oculta el chip si está vacío); el plato determinista lleva `prep_time` + fuente. |
| 4 | Escalado sin actualizar agua/instrucciones | **Confirmado y vigente** (canario) | `construir_comida` escala gramos y copia `pasos` tal cual (`deterministic_day.py:369, :402`); la biblioteca declara «el agua SÍ se mide» (`procedencia.sin_cantidades`); 24 menciones absolutas de agua en 21 pasos; ningún escáner la ve (V4 mide gramos, V6 exige cifra). Quinoa 54 g con «tres tazas de agua». | **Corregido**: `escalar_agua_en_pasos(pasos, factor)` — cifras y palabras («dos tazas y media», «un litro y medio», «media taza»), tazas a ¼, litros a 50 ml; NO toca proporciones («por cada taza de arroz») ni cucharadas; marca `_recipe_water_scaled`. Fuera de alcance: `apply_library_recipe` en el camino LLM no conoce el factor (ver §6). |
| 5 | «salmón» clasificado por «sal» | **Confirmado y vigente** | `deterministic_day.py:165-167` `any(k in n for k in _NO_ESCALAN)`: Salami, Salsa de tomate, Ensalada, Salmón, Bacalao salado, Ajonjolí quedaban SIN escalar; 15 plantillas DO afectadas; en «Salami guisado con yuca» el congelado era la proteína (elegido por 427 kcal, servía 574). Los otros dos SSOT de condimentos ya usaban frontera de palabra. | **Corregido**: `_NO_ESCALAN_RES` por palabra (con plural); `_retinol_preformado_mcg` usa `_RETINOL_ANIMAL` por palabra (tenía una copia más corta y por subcadena). |
| 6 | Nutrientes desconocidos → 0; sodio de la porción original | **(a) Parcial · (b) Confirmado y vigente** | (a) El compilador y el selector distinguen desconocido (`dish_registry.py:198-238`, `:744`); siguen coaccionando a 0: `graph_orchestrator._line_sodium_mg` (`:30275`, SSOT del autofix), `nutrition_db.py:909`, `deterministic_day._sodio_de` (`:513`, y no lee `nutrition_unknown`). (b) `_sodio_de(_t)` leía la ración BASE del registry mientras el plato se escalaba hasta 1,6× e inclinaba ±35 %: arenque 1.232 mg anotados / ~1.970 servidos. La medición que justificó `baf4163` («0 de 29 días sobre el techo») se hizo con esa cuenta. | **Corregido (determinista)**: `_sodio_de_comida` suma `sodium_mg_per_100g` × gramos servidos, cuenta las líneas sin dato (`_sodium_unknown_lines` en plato y día) y sólo cae a ración×factor si ninguna línea publica sodio. **Pendiente (LLM)**: `_line_sodium_mg` coerce a 0 sin marca (§6). |
| 7 | Cobertura desigual entre validadores; excepciones → listas vacías | **Confirmado y vigente** | Matriz validador×ruta en §4. Lo más grave: `deterministic_day.py:579-588` envolvía `clinical_backstop_for_meal` (fail-secure por dentro, `graph_orchestrator.py:16360`; y en el path degradado `cron_tasks.py:25511`) en `except: logger.debug` ⇒ «no pude evaluar» = «servible». Escáner culinario: 6 de 11 comprobaciones a mano. 13 `except Exception: return []` inventariados. | **Corregido (determinista)**: backstop y techo de retinol fail-secure (violación sintética); escáner completo vía `culinary_contract_scan`; «no medible» en `warning`. **Pendiente**: los `return []` del camino LLM son decisión de diseño (warn) — se documentan, no se cambian. |
| 8 | Sin representación del consumo entre pasos | **Confirmado y vigente; diferido a propósito por el repo** | Esquema de receta = `{franja, pasos}` (190/190); constituyentes planos sin referencia a paso; `culinary_coherence.py:1158-1161` lo nombra como cuarta clase no implementada; V6 asume «puede repartir» sin seguirlo; `canonical_recipe.py` se detiene en compras y macros por decisión del dueño (2026-09-06). | **Sin cambio de código.** Propuesta en §7 (modelo de asignación paso↔ingrediente, con coste). |
| 9 | Avisar/bloquear/no poder evaluar según la ruta | **Confirmado y vigente**, con un bug vivo adicional | No existe estado «no evaluable» en ningún guard: `shopping_calculator.py:9952-9954` y `:10412-10414` devolvían `[]`/`([], False)` ante excepción, también en `block`. **Bug vivo**: `graph_orchestrator.py:44564` `{_cul_cov:.0%}` con `None` (P1-MEASUREMENT-INTEGRITY) reventaba dentro del `try`: se perdía el aviso y en `block` `approved = False` nunca corría. `docs/coherence_surfaces_table.md` con líneas obsoletas y sin fila para «no evaluable». | **Corregido**: f-string None-safe (neutro en líneas); `_mark_guard_unevaluable` deja `plan_result["_shopping_coherence_unevaluable"] = {stage, error, detail, at}` y log `error`. NO bloquea a propósito (reintentaría el mismo cálculo hasta dead-letter). **Pendiente**: fila en la tabla de superficies y consumo del flag por `review_plan_node` (§6). |
| 10 | Alcance de las huellas | **Confirmado y vigente** | `catalog_fingerprint` = nombre + 10 columnas nutricionales (`dish_registry.py:133-138`): precio, alias, vida útil, `supermarket_products`, inventario, prompt, `random_seed`, modelo/temperatura, texto de pasos, perfiles culturales y knobs NO entran. Recompilación «en v1» con mismo `registry_version` (ES: `d0bd7b…`→`c0f939…` entre baf4163 y HEAD); `p1_do_despensa` cambió precio sin mover huella y el precio poda candidatos (`:815-823`, exclusión DOCUMENTADA). `MEALFIT_FIDELITY_GATE=warn` en prod. | **Sin cambio de código** (decisión de diseño con trade-off explícito). Propuesta §7: separar «huella de la petición» de «huella de la computación». |
| 11 | Métricas de personalización incompletas | **Confirmado y vigente** | Dos scorers: `horizon.fidelity_report` (anclas + repetición; con 0 anclas `n_checks = 2` ⇒ score 1,0 sin medir nada) y `plan_policy.measure_plan_against_policy` (sombra). Matriz en §5: de 11 dimensiones, medidas 5; capturadas y JAMÁS medidas: `scheduleType`, `cookingTime`, equipo, hogar, cultura, mercado, inventario y **porción de ancla** (P1-PORTION-HONORED la impone y nadie la mide). | **Sin cambio de código**. Propuesta §7 (códigos `anchor_portion_*`, `prep_time_over_budget`, `culture_share_*`). |
| 12 | Dependencias ausentes en la invalidación de proyecciones | **Confirmado y vigente** (`MEALFIT_PLAN_JOBS_ENABLED=1` en prod) | 4 sitios re-encolaban; sin re-encolar: `/shift-plan`, `/grocery-start-date`, `/restore`, `/restore-local`, `/regenerate-simplified`, `/recipe/expand` (quedaban `stale` para siempre) y **`/restock`, `/inventory/consume`, edición de precios, `householdSize`** (no tocan `plan_data` ⇒ `ready` mintiendo). La huella (`reprojection.py:19-34`) no incluye inventario ni precios aunque el read model los LEE. | **Corregido**: 6 hooks nuevos (`shift_plan`, `grocery_start_date`, `restore`, `restore_local`, `restock`, `inventory_consume`); `inventory_fingerprint` (Nevera + consumidos) entra en el dedupe y en la clave. Excluidos a propósito: `/recipe/expand` (sólo pasos) y `/regenerate-simplified` (el chunk re-encola al rellenar). **Pendiente**: precios de `supermarket_products`/`master_ingredients` y `householdSize` (§6). |
| 13 | Variación por orquestación, azar, memoria y datos | **Confirmado y vigente** | 18 fuentes inventariadas en §5: `random_seed` sin semilla inyectado en el prompt (`graph_orchestrator.py:7584`), 16 llamadas a `random` en el seeder, A/B de temperatura por Thompson, hedging por reloj, orden de `set` en prompts (sin `PYTHONHASHSEED` en `Procfile`), RAG/Dreaming, fecha, catálogo en vivo. Único RNG sembrado: `dish_library.py:127`. El merge de días SÍ es determinista (`:9479`). | **Sin cambio de código** (es la arquitectura del camino LLM). Propuesta §7. |
| 14 | Receta congelada vs aprobada vs validada por porción | **Confirmado y vigente** | Firma curatorial `automated` por `snapshot_hash` y por RACIÓN BASE (`cultural_curation_review_v1.json`); `_signoff_for` sólo lo lee el benchmark. (a) El sodio del plato escalado se validaba con la ración base (= H6b). (b) `/recipe/expand` reescribía una receta firmada en «exactamente 3 pasos» (`prompts/meal_operations.py:70-73`), perdiendo p. ej. el aviso de 74 °C, y nadie miraba `_recipe_source`. | **Corregido**: (a) con H6; (b) `/recipe/expand` devuelve la receta congelada sin LLM cuando el plato es de biblioteca (por `_recipe_source`, por la fila persistida o por pasos idénticos). **Pendiente**: re-firma tras recompilar (decisión humana, ya registrada). |
| 15 | Preferencias culturales y contratos distribuidos | **Confirmado y vigente** | Motor paramétrico (`PROFILES` es dato) pero: 11 puntos de cambio para una 7ª cocina (6 de ellos tests); mezcla libre bloqueada por `MAX_SECONDARY=2`/`MIN_MAIN_WEIGHT=0.5` y `profile_for_day` asigna UNA cocina por día aunque el blueprint guarda cultura por franja; **no existe «ninguna cocina»**: sin `cultureProfiles` cae a la del mercado (`cultural_profiles.py:210-211`); `dislikes` no llega a `template_candidates`; `super_personalization.cuisines` es un segundo canal contradictorio (sólo prompt); sin código de relajación cultural en `_REASON_COPY`. Una cocina sin país no puede existir (`country_for_profile`). | **Sin cambio de código**. Propuesta §7 (perfil `neutral`, `exclusions` al registry, código de relajación `culture_unavailable`). |
| 16 | Reserva del selector que abandona antes | **Ya corregido en `98f0bda`**, con residuos | En baf4163: `if … > techo and _reserva is None` ⇒ el SEGUNDO salado se servía de largo. HEAD ya lo saltaba (test `test_cada_candidato_salado_se_salta_no_solo_el_primero`). Residuos: `elegibles[:_empate_max()]` ANTES de rotar (los del puesto 11+ inalcanzables siempre); reserva «el primero», no «el menos salado»; `return None` del día ante franja desconocida. | **Residuos corregidos**: rotación antes del corte; reserva = menos salado; franja desconocida sigue cediendo el día al LLM (ahora por etiqueta canónica). |

**Resumen**: 11 confirmados y vigentes, 3 parciales (2, 6, y 16 en su enunciado literal), 1 ya corregido (16), 0 falsos positivos, 0 no verificables en lo que se pudo leer. Lo que NO se pudo verificar: el texto exacto y las aserciones de las 14 sondas.

## 2. Las 14 sondas

**Bloqueado: el archivo no está.** Se deja aquí el criterio con el que se auditarán al llegar, hallazgo por hallazgo:

- Una sonda «demuestra» un hallazgo sólo si su fixture reproduce la FORMA REAL del dato que llega al callee. El caso paradigmático es H1: cualquier sonda que pase `{"slots": [...]}` a `build_day_for_skeleton` prueba el algoritmo pero NO el defecto entregable (que era el contrato del esqueleto). El test nuevo `test_h1_el_dia_real_pide_al_registry_las_franjas_y_la_familia_del_esqueleto` usa la forma de `DaySkeletonModel`.
- Sondas basadas en AST/regex sobre el fuente: valen para «existe/no existe» y no para «se ejecuta»; deben acompañarse de una ejecución (H3, H7, H9 y H12 tienen aquí las dos variantes).
- Sondas con mocks de DB: si el hallazgo depende del dato vivo (H6a, H10b, H14a), la sonda con catálogo sintético no permite concluir sobre producción; aquí se midió con el catálogo real donde hizo falta (agua: 24 menciones; salami: 15 plantillas; sodio: arenque 1.232 vs ~1.970).
- Las sondas que «confirman el defecto» (aserciones sobre el comportamiento erróneo) no se han convertido en tests: los 69 tests nuevos afirman el comportamiento esperado.

## 3. Problemas adicionales encontrados (no estaban en los 16)

| Prioridad | Problema | Evidencia | Estado |
|---|---|---|---|
| P0 | **Premisa falsa** «el día determinista no pasa por `assemble_plan_node`» en 4 docstrings y 2 commits; justificó dos P-fixes | `graph_orchestrator.py:46905-46909` (aristas incondicionales), `:39985/:40445` (autofix de sodio corre sobre esos días) | Corregido (docstrings); los P-fixes se mantienen como defensa en profundidad |
| P0 | Gate culinario muerto en `block` por `{_cul_cov:.0%}` sobre `None` (dentro del `try`, log perdido) | `graph_orchestrator.py:44564`; reproducido: un elemento no-string en `ingredients` ⇒ `scan_coverage`→`None` ⇒ `TypeError` ⇒ «scan no-op» | Corregido |
| P1 | El día determinista traía DOS tablas de reparto (`_REPARTO` vs `MEAL_SLOT_SPLITS`) — la lección `P1-DIET-CANON-SSOT` repetida | `deterministic_day.py:102` vs `nutrition_calculator.py:401` | Corregido (se elimina `_REPARTO`) |
| P1 | `graph_orchestrator.py` está EXACTAMENTE en su tope (53.100 = `test_p1_deterministic_day_wired.py:29`): cualquier fix ahí compite por líneas | `wc -l` = 53100 | Documentado; las dos ediciones fueron neutras |
| P1 | `_retinol_preformado_mcg` usaba una copia más corta de `_RETINOL_ANIMAL` y por subcadena | `deterministic_day.py:530-531` | Corregido |
| P2 | `docs/coherence_surfaces_table.md`: todas las referencias de línea obsoletas; sin fila «no evaluable» | verificación de agente | Pendiente (doc) |
| P2 | `docs/plan_policy_f3.md:91` afirma que ningún ancla se valida contra el catálogo del país; `ARQ27-P1-07` lo cerró | `docs/audits/2026-09-06-generacion/ESTADO-IMPLEMENTACION.md` | Pendiente (doc) |
| P2 | `dish_registry` publica `nutrition_per_serving` sub-contado cuando un constituyente no tiene dato, indistinguible de completo salvo por `nutrition_unknown` | `dish_registry.py:436-441`; hoy 0 plantillas con sodio desconocido (sólo fósforo: 5 CO, 2 MX) | Latente; documentado |

## 4. Matriz validador × ruta (estado tras esta pasada)

| Validador | LLM (assemble→review) | Determinista | Degradado (Edge) | Swap / tool | Recalc / restock |
|---|---|---|---|---|---|
| Alérgenos (`_scan_allergen_violations`) | ✅ | ✅ backstop **fail-secure** (antes fail-open) | ✅ fail-secure | ✅ | — |
| Dieta | ✅ fail-secure | ✅ backstop | ✅ | ✅ | — |
| Culinario V1–V7 | ✅ 11/11 (warn) | ✅ **11/11** (antes 6/11) | ⚠️ V1/V2 | ❌ | ❌ |
| Juez culinario LLM | ✅ (off en prod) | ❌ | ❌ | ❌ | ❌ |
| Guard de coherencia de compras | ✅ block; **marca «no evaluable»** | ✅ (mismo nodo) | — | ⚠️ warn | ⚠️ warn |
| Techo de retinol | ❌ | ✅ **fail-secure** | ❌ | ❌ | ❌ |
| Techo de sodio del día | ✅ autofix (coerce 0) | ✅ propio, **sobre el plato servido** | ❌ | ✅ | — |

## 5. Matrices de H11 y H13 (resumen)

**Personalización (H11)** — capturado / usado / medido / en el panel «aplicamos»: gustos ✅✅✅✅ · disgustos ✅ parcial (no llegan al registry) · **parcial** (sólo scorer sombra) ❌ · porciones ✅✅ **❌** relajación · horario ✅ sólo texto ❌❌ · tiempo disponible ✅ parcial (`batch_cooking`) **❌** parcial · equipo opt-in sólo prompt ❌❌ · presupuesto ✅✅ parcial relajación · cultura ✅✅ **❌** ✅ · mercado ✅✅ ❌ indirecto · inventario ✅✅ ❌❌ · alérgenos/dieta/condiciones ✅✅ otro scorer ❌ · hogar ✅✅ ❌❌ · ciclo/congelador ✅✅✅✅.

**Variación (H13)** — 18 fuentes; las de mayor impacto: (1) `random_seed` sin semilla en el prompt, (2) seeder con 16 llamadas a `random` global, (3) A/B de temperatura, (5) hedging por reloj, (7) orden de `set` en prompts sin `PYTHONHASHSEED` en `Procfile`, (12-13) RAG + Dreaming, (14) fecha, (15) catálogo en vivo. El camino determinista es el único sin `random` ni `datetime.now`.

## 6. Cambios implementados y por qué

| Archivo | Cambio | Hallazgo |
|---|---|---|
| `deterministic_day.py` | `_franjas_del_dia` / `_etiqueta_a_franja` / `_fracciones_por_franja` / `_familias_del_dia` sobre los SSOT del camino LLM; `_REPARTO` eliminado | H1 |
| `deterministic_day.py` | `_candidatos`: CandidateSet fijado ∩ filtros vivos, unión por familia con fallback dicho; `market_country`, `required_nutrients`, `_dur_kwargs`; cocina por `day_index`; rotación absoluta; `_candidate_source`, `_day_index` | H2 |
| `deterministic_day.py` | `prep_time` del registry (receta/técnica) | H3 |
| `deterministic_day.py` | `escalar_agua_en_pasos` + `_recipe_water_scaled` | H4 |
| `deterministic_day.py` | `_NO_ESCALAN_RES` por palabra; retinol por palabra con `_RETINOL_ANIMAL` | H5 |
| `deterministic_day.py` | `_sodio_de_comida` (plato servido, líneas sin dato contadas) | H6b / H14a |
| `deterministic_day.py` | `verifica_comida`: backstop y retinol fail-secure; escáner completo; avisos en `warning`; docstrings sin la premisa falsa | H7 / H2 |
| `deterministic_day.py` | rotación antes del corte; reserva menos salada (anclas de tests conservadas) | H16 |
| `recipe_library.py` | `prep_time_for_meal`, `fill_prep_time` | H3 |
| `graph_orchestrator.py` (neutro en líneas) | `assemble`: sin «15 min» inventado; `review`: f-string None-safe | H3 / H9 |
| `shopping_calculator.py` | `_mark_guard_unevaluable` en los dos `except` del guard | H9 |
| `shopping/projection/reprojection.py` | `inventory_fingerprint`; dedupe y clave con Nevera; `payload.inventory_fingerprint` | H12 |
| `routers/plans.py` | 6 hooks de re-encolado; `/recipe/expand` respeta la receta congelada | H12 / H14b |
| `app.py` | marker `P1-AUDITORIA-ARQ-VERIFICADA · 2026-09-11` | — |
| `tests/test_p1_auditoria_arq_verificada.py` | 69 tests, uno o más por hallazgo corregido | — |

**Decisiones de diseño tomadas (y por qué no la alternativa):**
- Reparto y etiquetas se REUTILIZAN de `graph_orchestrator._canonical_slot_fractions`/`_SLOT_KEY_MAP` (import perezoso) con respaldo local sobre `MEAL_SLOT_SPLITS`: cero tablas nuevas. Extraer esas dos piezas del god-file a `nutrition_calculator` sería lo correcto y es la propuesta de refactor; no se hizo aquí porque el god-file está en su tope y mover código exige su propio P-fix con test.
- «No evaluable» en el guard NO bloquea: bloquear por un fallo del propio guard reintentaría el mismo cálculo determinista hasta la dead letter (lo mismo que impide encender `MEALFIT_GUARD_UNDERSUPPLY_SEVERE`). Se marca y se loguea como error; el consumo del flag por `review_plan_node`/dashboard queda como pendiente porque el god-file no tiene líneas.
- El agua se escala en la COSTURA del camino determinista (donde el factor existe) y no en el dato: reescribir 21 pasos caducaría la firma curatorial y sería una decisión editorial del dueño. `apply_library_recipe` (camino LLM) no conoce el factor: ahí el agua sigue sin escalar y queda dicho.
- `_sodio_de` se conserva (su test lo fija) como respaldo; la cuenta del día usa el plato servido.

## 7. Pendiente, riesgos y propuestas de mayor alcance

**Correcciones acotadas que quedan (no cabían en esta pasada o tocan el god-file):**
1. `graph_orchestrator._line_sodium_mg` coerce a 0 sin marca (H6a, camino LLM) — devolver `(mg, desconocido)` y que el autofix reporte la incertidumbre. Exige líneas en el god-file: extraer primero.
2. `review_plan_node` debe leer `_shopping_coherence_unevaluable` y dejarlo en el veredicto/telemetría (H9); fila «no evaluable» en `docs/coherence_surfaces_table.md`.
3. Re-encolar la proyección al cambiar precios (`routers/supermarket.py`, migraciones de `master_ingredients`) y `householdSize` (H12) — hoy sólo la Nevera entra en la huella.
4. `apply_library_recipe` en el camino LLM: derivar un factor implícito (gramos del constituyente ancla / gramos de la ración) o marcar `_recipe_water_unscaled` (H4).
5. Docs obsoletos (`coherence_surfaces_table.md`, `plan_policy_f3.md:91`).

**Propuestas (beneficio · riesgo · migración):**
- **Huella de la computación (H10, H13).** Un `computation_hash` por chunk = `input_hash` + `snapshot_hash` real usado + `catalog_fingerprint` extendido (precio, alias, vida útil) + `prompt_version` + `model_id` + temperatura + semilla. Beneficio: «mismo hash ⇒ misma computación» deja de ser falso. Riesgo: más jobs `stale`; mitigar con dos huellas (identidad del run vs. de la salida). Migración: columna nueva en `plan_generation_runs`, sin tocar `input_hash`.
- **Semilla determinista donde el producto quiera repetibilidad (H13).** `random.Random(hash(user_id, plan_id, day_index))` en seeder y `random_seed` del prompt; `PYTHONHASHSEED` fijo en `Procfile`. Beneficio: dos generaciones del mismo run coinciden salvo el LLM. Riesgo: menos variedad entre runs del mismo usuario si se siembra sin `plan_id`.
- **Asignación paso↔ingrediente (H8).** Extender la receta congelada con `pasos[i].usa: [{ingredient_id, fraccion}]` sólo para las 190 DO (se escribe una vez); V6 pasa de «puede repartir» a suma exacta. Coste: revisión editorial de 190 recetas; la firma curatorial caduca y hay que re-firmar (decisión humana ya pendiente).
- **Métrica de personalización completa (H11).** Añadir a `fidelity_issues` los códigos `anchor_portion_below/above`, `prep_time_over_budget` (con `prep_minutes_est`, que ahora sí se lee), `culture_share_off_target`, `equipment_unavailable`; y que el score no pueda ser 1,0 con `n_checks = 2`. Beneficio: promover `MEALFIT_FIDELITY_GATE` a `block` con una métrica que informa.
- **«Ninguna cocina» y exclusiones en el registry (H15).** Perfil `neutral` (sin biblioteca dominante, reglas de franja blandas), `diet.exclusions` pasadas a `template_candidates`, y código de relajación `culture_unavailable` en `_REASON_COPY`. Beneficio: la preferencia explícita se respeta o se explica; hoy se sobrescribe en silencio.
- **Refactor mínimo del god-file.** Extraer `_SLOT_KEY_MAP` + `_canonical_slot_fractions` a `nutrition_calculator` y `_line_sodium_mg`/`_meal_sodium_mg` a `nutrition_db`: libera líneas y elimina los imports perezosos del god-file desde `deterministic_day`.

**Riesgos de esta pasada:**
- El día determinista ahora sirve 3/5/6 comidas y filtra por familia y mercado: para el canario del dueño cambia QUÉ come (más días pueden caer al LLM si el pool no deja candidatos: se registra `vivo_sin_familia`). Es el comportamiento pedido, pero es un cambio visible.
- El escáner culinario completo puede rechazar más candidatos deterministas que la lista de 6 (más días al LLM). Medir con `pipeline_metrics` tras el despliegue.
- `_canonical_slot_fractions` normaliza a 1,0: un esqueleto de UNA franja recibe el 100 % de las kcal (los tests lo reflejan).
- `inventory_fingerprint` añade una lectura de Nevera por re-encolado (ya la hace el read model; el coste es una consulta más por mutación).

## 8. Verificación ejecutada

- Suite del día determinista (12 ficheros): **170 passed** tras los cambios.
- `tests/test_p1_auditoria_arq_verificada.py`: **69 passed**.
- Gate completo del backend (`scripts/run_ci.ps1 -SkipFrontend`): ver el resumen final del encargo (resultado y fallos, si los hubo, con su clasificación).
- Reproducciones contra el catálogo vivo: sazón/hummus/pimentón (lista de compras), agua en 21 pasos, sodio del arenque, 15 plantillas con «sal» falsa.
- **No verificado**: generación real punta a punta con el LLM, continuaciones (bloques 2+) y recuperaciones en producción — exigen VPS/LLM y quedan como comprobación bloqueada hasta el despliegue en canario.

**Garantías concretas**: lo que los 69 tests afirman es que, con un `DaySkeletonModel` real, el día determinista arma las franjas del esqueleto con el reparto SSOT y la familia del pool, pasa al registry los mismos filtros que `horizon`, honra el CandidateSet fijado, no congela salami, carga el sodio servido y dice lo que no sabe, rechaza cuando no puede evaluar lo clínico, escala el agua medida, lleva su tiempo del registry, no inventa «15 min» en `assemble`, no mata el gate culinario por un formato, marca el guard no evaluable, re-encola la proyección en seis mutaciones y no reescribe recetas firmadas. **Límites**: nada de esto prueba la calidad del plato ni la exactitud del catálogo; el camino LLM conserva sus fuentes de variación y sus `return []` por diseño; el informe original y sus sondas siguen sin auditar.
