# Plan de lo que falta por arreglar — BioBoros, 2026-09-11

Inventario consolidado de trabajo abierto, con fuente. Nace de la verificación de la auditoría de arquitectura (`auditoria_arquitectura_verificacion_2026_09_11.md`) y del barrido de planes, specs, docs, memoria y knobs apagados. **Nada aquí está inventado: cada línea cita dónde está escrito.** Las prioridades son una propuesta de orden; las decisiones marcadas «dueño» no las cierra código.

> **Para el siguiente agente (2026-09-13):** los cinco cambios que no dependen del dueño (extracción del god-file, F8, F6, C7 medible, mediciones B6/E5/D7) están escritos como lotes 32-36 ejecutables, con protocolo, cifras y criterio de terminado, en [`plan_agente_lotes_32_36_2026_09_13.md`](plan_agente_lotes_32_36_2026_09_13.md).

> **Para el siguiente agente (2026-09-14):** los lotes 32-37 están cerrados. Lo que queda y puede cerrar código (la anotación del dueño al instrumento, detectores para `coccion_faltante`/`seco_sin_coccion`, calibración medible del juez, la tormenta de reintentos del catálogo, worktrees viejos, re-mediciones con fecha) está en [`plan_agente_lotes_38_43_2026_09_14.md`](plan_agente_lotes_38_43_2026_09_14.md), con las cifras medidas el 14-sep. Las diez decisiones 👤 del plan (juez, E5-B, claras, V7a, B7, equipo, D8, G1, F9) las delegó el dueño el 14-sep y están elegidas y razonadas en [`decisiones_dueno_2026_09_14.md`](decisiones_dueno_2026_09_14.md); las aplica el lote 44.

## Estado (se actualiza al cerrar cada ítem)

| Ítem | Estado | Dónde |
|---|---|---|
| Panel · el tiempo de cocina | ✅ 2026-09-15 · el panel «solicitaste / aplicamos / por qué» dice cuándo el plan pasa del tiempo pedido | `P1-PLAN-LOTE-54`: el revisor medía `prep_time_over_budget` (de 6 a 10 comidas por plan en las 5 pruebas del dueño con «Nada», de hasta 70 min) y el panel no lo mostraba. Ahora cuenta como un ajuste más, con la cifra real y el botón «Cambiar Plato»; con el tope del backend (10), «al menos 10». Es honestidad, no un arreglo: el tiempo sólo bajará cuando la biblioteca tenga platos rápidos, y el estimador de minutos (el 55, medido y propuesto al dueño) cuenta como instantáneos los pasos al fuego sin minutos. |
| Medida · decimales y aislamiento de los tests | ✅ 2026-09-15 · el punto entre cifras no parte la oración; el conftest pre-importa lo que los tests suplantan | `P1-PLAN-LOTE-52`: la frontera de oración del contrato culinario partía «0.23 g de Sal» en «0» y «23 g de sal», así que V4 y el reparador leían 23 g, y «57.2 g» como 2 g; `dish_structure` y la plantilla «a la plancha» del cerrador tenían el mismo corte. Medido sobre los 11 planes de los últimos 21 días: 69 de 172 comidas llevan alguna mención así y el scan y el reparador dan lo mismo con una frontera y con otra. Hoy es inerte; mañana no, porque `formatear_cantidad` escribe los decimales con punto. Aparte, 20 tests legacy dejaban módulos VACÍOS del backend al importarse: `test_p0_b_synthesis_per_user_circuit_breaker` → `test_p1_plan_display_i18n` → `test_p1_plan_lote_15` daban 15 fallos y 2 errores, y cada fichero pasaba solo. El conftest pre-importa ahora `agent`, `memory_manager`, `services`, `ai_helpers` y `auth` (+1,4 s por sesión), más `apscheduler.triggers.cron`, que 19 tests dejaban con `CronTrigger=object` (con `test_chunked_learning_propagation` delante, los tests de registro del cron zombie y del de reservas huérfanas daban `TypeError`). Un test vigila que todo módulo del backend que un test suplante esté en la lista y que ningún módulo de terceros que el backend importa siga siendo un stub en la sesión. |
| Prueba RD 5 · plan a059d7bb | ✅ 2026-09-14 · la identidad del plato hasta el final; el cerrador lee el día y la receta; tope de huevos en la compra; planes recientes con días; tiempo como falta; texto | `P1-PLAN-LOTE-49`: la mejor de la serie (144 s, 3/3 deterministas, 12/12 de biblioteca, juez 0), pero guacamole con 5 g de aguacate, maní tostado con 5 g de maní, huevo repetido por el cerrador, «3 claras» en la lista y huevo entero en la compra, 6/12 platos de un plan anterior, 7/12 comidas sobre el tiempo con una cena de 65 min. Réplica de solo lectura sobre el plan entregado a059d7bb: en la cola del guardado el día 1 recupera 38 g de aguacate en el guacamole (tenía 5), 8 g de maní (5) y 7 g de mantequilla de maní (3), y pasa del 89 % al 92 % de sus kcal y del 71 % al 86 % de su grasa; los días 2 y 3 no cambian (el 2 ya está en su techo). La primera versión medía los gramos con el lector de la lista del contrato, que lee «57.2 g» como 2 g, y en la réplica «subió» la soya de la cena del día 3 de 57 a 18 g: ahora mide con el lector de la base y nunca baja. La falta de tiempo se midió en réplica del run (7 días, «Nada», contra los planes recientes) con cuatro pesos, y ninguno mejora el tiempo sin romper otra regla: con 0,35/1/1,5 los almuerzos y cenas bajan de 36,4 a 35,0 min de media y el primer bloque deja de hacer saltar la autocrítica, pero el segundo pasa de «guiso ×3» a «pollo ×4»; con 0/1/2,5 desaparece la cena de 65 min (máximo 35, media 28,9) pero dos platos pasan su tope de 7 días; con 0/0,5/2,5 el máximo baja a 45 y un plato pasa el tope. La biblioteca DO tiene 2 de 63 almuerzos y 1 de 56 cenas de 10 min: el armador no tiene con qué. Queda APAGADA, con los pesos 0,35/1/1,5 (los únicos que no pasan el tope de 7 días), lista para cuando haya platos rápidos. |
| Prueba RD 4 · plan 358a2cdf | ✅ 2026-09-14 · lo que añaden los cerradores respeta la receta; el armador prefiere el plato que llega al piso; la cola se re-mide | `P1-PLAN-LOTE-48`: lo del 47 funcionó (11/12 comidas de biblioteca, 2/3 días deterministas, aprobado al primer intento, 203 s), pero los cerradores colgaban otra base (arroz junto al mofongo), licuaban queso en un jugo de chinola y compraban «queso» a secas; el cambio del arenque borraba la frase del locrio; 335 g de chinola; y un día que el barrido ya había dejado limpio fue igual al LLM. Réplica de solo lectura del día determinista sobre el blueprint del run del plan 358a2cdf (7 días en dos bloques, «Nada» de tiempo, presupuesto bajo, contra los planes que eran recientes a esa hora): sin el piso, 5 de 28 comidas quedaban por debajo del piso de proteína de su franja (el desayuno medio, al 76 % de su objetivo). Con el piso pesando 1,5 quedaban 0 de 28, pero servía «Sardinas en lata con casabe» 4 veces en 7 días (3 en su bloque): el piso le ganaba a la cuota de repetición que el formulario («equilibrado») pide respetar. Con 0,4 —por debajo de la cuota— quedan 3 de 28 y la variedad no se mueve (0 sobre el tope de 7 días, 2 repetidos en bloque, 21 platos distintos, 6 de planes recientes; minutos medios 28,2 → 29,3). Los cambios de receta, comprobados sobre los platos del plan: la batida «con queso cottage» compra queso cottage (antes, «queso» ⇒ queso blanco), el jugo de chinola sirve el queso al lado, el locrio conserva «el filete de pescado blanco (en trozos)… y añade el arroz blanco», la chinola baja de 335 a 120 g y el piso de calorías escala el plátano del mofongo (×1,5 como mucho) en vez de colgarle arroz. |
| Prueba RD 3 · plan d8b10b05 | ✅ 2026-09-14 · re-elegir, no reescribir; el armador conoce la autocrítica; parches | `P1-PLAN-LOTE-47`: aprobado al primer intento y sin repetir planes anteriores, pero la autocrítica reescribió con el LLM los 3 días deterministas y el corrector les quitó la procedencia (0/12 comidas de biblioteca). Ahora el día señalado se re-elige con el armador y sólo el que no mejora va al LLM; lo que el LLM deja igual vuelve con su receta; el armador aplica las reglas fijas de la autocrítica. Parches: el paso que enfría no lleva tiempo, el casabe se tuesta, el queso no se revuelve. Simulación de solo lectura del día determinista sobre el blueprint del run del plan d8b10b05 (7 días en dos bloques, tiempo «Nada», contra los 2 planes que eran recientes a esa hora): con las reglas del lote 46 la autocrítica saltaba en los dos bloques —el primero por 4 detectores: avena en 2 días, almuerzo y cena con yuca, avena en desayuno y merienda, huevo dos veces el día 3—; con las del 47 el bloque 1 queda limpio y el 2 salta sólo por yuca en 2 días. Platos de planes recientes 5 → 2, exceso sobre el tope de 7 días 0 → 0, platos distintos 21 → 21, minutos medios 20,9 → 25,9 (el coste: con 10 min la biblioteca no tiene alternativa en almuerzos y cenas). Sobre los días del lote 46 con la sugerencia real del evaluador, la re-elección rehízo los 3 días sin LLM (el 3 por el barrido) y la autocrítica quedó en «yuca en 2 días». La primera medición, con sólo tres reglas, llevó el pollo a 3 días de 3 (monotonía): por eso entraron la proteína pesada, el plato-base, la franja y la base ligera; y la cuota de repetición pesa a medias porque, entera, empataba con lo que hace saltar la autocrítica. |
| Prueba RD 2 · plan 63eedc6b | ✅ 2026-09-14 · repetición entre planes, identidad del plato, parches | `P1-PLAN-LOTE-46`: tras el lote 45 el revisor rechazó los 3 intentos por repetición contra los planes recientes y el día determinista armaba lo mismo (354 s, entregado sin aprobar). Ahora el día prefiere lo no reciente y respeta el tope del bloque, la familia del blueprint va a la comida principal con la puerta de proteína del día, y el revisor no reintenta lo que un día determinista arma igual. El ingrediente que da nombre al plato no se recorta y, si falta, vuelve al 25 % (`identidad_plato`); el cerrador ya no manda hervir el queso y el autofix no reescribe recetas congeladas. Simulación de solo lectura del día determinista, 7 días en dos bloques sobre el blueprint del último run del dueño (tiempo «Nada»), contra sus 3 planes más recientes: platos de esos planes 15 → 5 de 28, repeticiones dentro del bloque 4 → 2, días con proteína repetida 4 → 0, platos distintos 19 → 21, minutos medios 19,6 → 20,9. Identidad sobre una copia del plan 63eedc6b: vuelven 4 alimentos (+8 g de maní, +24 g de leche evaporada, +6 g de dátiles, +38 g de aguacate) y la grasa de los tres días queda entre el 91 % y el 107 %. La primera versión (piso del 50 % y subir también lo que quedó pequeño) tocaba 7 platos, llevaba la grasa al 116-122 % y el salami de 5 a 41 g: por eso el piso es del 25 % y sólo vuelve lo que falta. |
| Prueba RD 14-sep · plan 40535829 | ✅ 2026-09-14 · receta en su orden, día determinista con blueprint y tiempo, sin reintento inútil | `P1-PLAN-LOTE-45`: el dueño generó con su perfil (ganar músculo, 30 d, compra mensual, presupuesto bajo, tiempo «Nada») y los 3 días salieron del día determinista. Medido: el reparador del contrato sacaba de su orden 149 de 193 recetas de biblioteca (281 oraciones; los 5 `paso_incoherente` del juez) → 0 rotulando el pilar en su sitio (`recipe_order`, `MEALFIT_RECIPE_TDF_IN_PLACE`), y `repair_stage_diff` cuenta el desorden; el pool del planificador rechazaba HIGH al día determinista (3 intentos, 427 s) → fuera del conteo; la memoria del reintento no veía los días reciclados (Mofongo ×2) → sembrada; familia del blueprint y tiempo de cocina al selector: sobre el blueprint real 32 → 17 min, 10 → 6 platos fuera de presupuesto, 4 → 1 repetidos. Los candidatos fijados por franja (`1:lunch` vs `1:almuerzo`, inertes) se leen detrás de un knob APAGADO por medición. |
| Tarea 13-sep · tormenta del catálogo | ✅ 2026-09-14 · knob `MEALFIT_CATALOG_NEGATIVE_CACHE_S` (30 s) | `P1-PLAN-LOTE-41`: medido antes — `horizon.build_blueprint` (perfil del landing, 14 d) hace 32.676 llamadas a `get_master_ingredients` (32.564 con el `_EFF` de la CI) y sin pool CADA una era una línea de error (32.676) y, con un pool que falla, un intento de conexión (32.676): `catalog_capability` sólo cacheaba snapshots no vacíos y `get_master_ingredients` no sellaba nada. Hecho: caché negativa corta en `get_master_ingredients` (ramas «sin pool» y «excepción»; error UNA vez por ventana, el resto a DEBUG con conteo; `_master_cache_ts` jamás se sella; el pool se mira ANTES que la ventana, así que uno que aparece o cambia la anula), `invalidate_master_cache` limpia el sello, y `catalog_capability` cachea su `None` por país con la misma ventana (`None` sigue siendo «desconocido»). Medido después, misma vara: 0 errores y 0 intentos durante el blueprint (1 y 1 por ventana en todo el proceso), 112 llamadas absorbidas (`dish_cost.tabla_de_precios`), 0,92 → 0,37 s. NO se hizo: cachear el vacío en `dish_cost` (sus 112 llamadas ya salen gratis), sellar la rama «no-lista» de P1-CATALOG-INDEX-NO-STICKY (nunca observada en prod), ni invalidar el snapshot POSITIVO de `catalog_capability` al editar el catálogo (gap previo, fuera del lote) |
| C7 | 📏 2026-09-13 · parte medible · 👤 presupuesto y cocinado real | `P1-PLAN-LOTE-35`: variedad perceptible por firma de preparación (día determinista 30 d: 7 d 18 nombres / 18 platos / 0 renombrados; 30 d 43 nombres / 43 platos / 0 renombrados; los planes del modelo disponibles tienen [3, 4] días: sin ventana de 7), presupuesto de reparación (corpus p50 3 / p95 7 capas por comida, 59.4 % con ≥ 3; propuesta `MEALFIT_RECIPE_REPAIR_BUDGET` = 8, NO implementada) y deriva (`--desglose` del bench con cobertura por superficie, `judge_violation_rate.py --por-pais`). CUL-P2-04 y P3 fuera: panel/producto. Doc: `culinary_coherence.md` §C7 |
| F6 | ✅ 2026-09-13 · barrido fila a fila · 👤 fuentes nacionales en PDF | `P1-PLAN-LOTE-34`: medido primero — `scripts/catalog_fdc_sweep.py` compara identidad (nombre ↔ descripción USDA) y valores (P/C/G/kcal con tolerancia absoluta) en las **288** filas con `fdc_id`: 214 coinciden, 19 mal apuntadas, 54 a revisar, 1 sin respuesta. Revisadas a mano una a una: **22 ids apuntaban a OTRO alimento** con los valores buenos (Tamarindo → verdolaga, Hígado de res → T-bone, Leche de almendras → un experimento con tomates, Salmón → el de granja…), 11 proxies no declarados, 7 filas con valores sin la fuente que decían, 17 kcal por Atwater general anotadas, 2 glosas. Los micros NO eran del id equivocado (0 de 19). Migración `p1_plan_lote_34_catalogo_fdc_2026_09_13.sql` (sólo procedencia, ningún valor; 0 ids compartidos). Las 9 en proxy siguen en proxy: USDA no las tiene y las tablas nacionales (SMAE, INCAP, TCAC 2018) son PDF — bajarlas es decisión del dueño. Doc: `catalog_provenance_audit.md` |
| E7 · P2-01 | ✅ 2026-09-13 · 1.ª extracción · tope 53.100 → 52.600 | `P1-PLAN-LOTE-32`: medido primero — 53.099 de 53.100 líneas; las funciones grandes leen 50-80 símbolos del módulo cada una, el bloque de resiliencia LLM sólo sus knobs (AST). Movidos tal cual a `llm_concurrency.py` (semáforos, presupuesto, 2 knobs) y `llm_circuit_breaker.py` (breaker distribuido, breaker best-effort, `LLMCircuitOpenError`); el grafo re-exporta y conserva la política (instancias, registro per-modelo, knobs `MEALFIT_CB_*`). El logger conserva su nombre; 6 tests re-apuntados, hallados comparando literales, regex y conteos de la suite contra el grafo de antes y el de después (dos parches del breaker ya no alcanzaban nada antes de mover). **51.956 líneas (−1.143)**, tope SSOT 52.600. Siguiente candidato: la caché LLM persistente (sin medir). Detalle: `arq30_e5_e7_diseno_canario.md` §P2-01 |
| E6 | ✅ 2026-09-12 · P1-03 medido + allocator mínimo · knob ON desde `P1-PLAN-LOTE-21` · P1-04/05 diseño | `P1-PLAN-LOTE-20`: una franja sin candidato no dejaba rastro; ahora el blueprint anota `registry.empty_slots` (con `rescuable_by_family`) y la rebanada lleva los suyos. Medido con `scripts/measure_horizon_slots.py` (6 mercados × 25 perfiles del landing × 4 escenarios, 600 blueprints, 49.200 franjas): **4.311 vacías (8,8 %), 4.211 tendrían plato con OTRA familia** — casi todas en compra mensual sin congelador (4.076): del día 8-9 Res/Cerdo/Pollo no tienen almuerzo ni cena que aguante y el round-robin se los asignaba igual; 100 huecos de biblioteca (desayuno sin lácteo/gluten/huevo de larga duración, mercados beta). El allocator URGE ⇒ versión mínima tras `MEALFIT_HORIZON_VIABLE_FAMILY` (default off): la familia del día pasa a la del pool que cubre MÁS franjas, determinista, movida en `d["protein"]` para que candidatos, prompt, sembrador y fidelidad vean lo mismo; anotada en `family_reassignments`. Encendido: **4.311 → 100 vacías (0,2 %)**, 2.315 días reasignados. Apagado = byte-idéntico. **`P1-PLAN-LOTE-21` (2026-09-12)**: el dueño delegó y el knob pasa a ON por defecto en el código (sin tocar el `.env` del VPS; sin cohorte: los runs en vuelo conservan su blueprint y sólo los nuevos lo ven; vuelta atrás `MEALFIT_HORIZON_VIABLE_FAMILY=0`). **Dueño**: el hueco del desayuno alérgico. P1-04 (autoridad de porciones) y P1-05 (commit validado) siguen en diseño (lote 10): mueven autoridad y se construyen bajo go |
| E5 | ✅ 2026-09-12 · fase A (sombra) · cohorte decidida (el dueño) pero la fase B NO entra: su gate no pasa, 21 de ≥ 30 planes y divergencia 5,6-20,6 % por plan (`P1-PLAN-LOTE-61`) | `P1-PLAN-LOTE-19`: la sombra de la lista CANÓNICA corre al final de `run_shopping_coherence_guard` (las 6 superficies, con el MISMO multiplicador efectivo que el guard, 0 líneas en el god file) y persiste sólo la comparación en `pipeline_metrics` (`canonical_shopping_shadow`); espeja la política que la lista ya lleva sellada (rendimientos, nevera) y usa la identidad del guard; no toca `plan_result` (el guard del expand admite exactamente este módulo). Knob `MEALFIT_CANONICAL_SHOPPING_SHADOW`; gate ≥ 30 planes con `parse_fail` < 1 % y divergencia < 5 % (`scripts/measure_canonical_shadow.py`, `--offline` mide ya). Primera medición (5 planes): 731 líneas, **0 fallos de parseo**, 214 comparables, 24 divergentes (11,2 %), 0 «sólo canónica» — y los divergentes son los TOPES realistas del agregador (rábano 524 → 50 g, tomate 3,5 kg → 750 g) y densidades (chinola, maní), no el parser: la fase B necesita que la canónica lleve esos topes o que el dueño los mueva. Veredicto NO CONCLUYENTE hasta 30 planes. **Dueño**: cohorte de B y política de topes **`P1-PLAN-LOTE-36` (13-sep)**: re-medido — 0 sombras en `pipeline_metrics` en 30 días (no se ha generado un plan desde el 09-09); `--offline` sobre los 5 planes vivos repite 0 fallos de parseo y 24/214 divergentes (11,2 %). NO CONCLUYENTE: 5 de 30 planes |
| C0 | ✅ 2026-09-12 · corpus fijo + etiquetas binarias (09-07) · 👤 rúbrica | `P1-PLAN-LOTE-18`: la línea base culinaria ya se congela sobre un corpus FIJO, no sobre la ventana viva. `culinary_corpus.py` congela lo que las capas leen (`days`, `_culinary_judge_history`, estado) + el catálogo del índice, con huella de contenido (orden y claves fuera; un paso o un alias dentro; un fichero editado a mano se rechaza); `scripts/congela_corpus_culinario.py` (sólo SELECTs) lo escribe y `culinary_baseline.py --corpus F --congelar/--verificar` mide, congela (`docs/culinary_baseline_<fecha>.json`) y comprueba reproducibilidad (exit 0/3/4); `--congelar` sin corpus se niega. Congelado 2026-09-12: 5 planes, 64 comidas, catálogo 349, huella `087cfc31d3105f79`, REPRODUCIBLE ×2; `computation.reglas_huella` separa el efecto del código del del cron. «ids + estado» no bastaba: la purga del 09-11 se llevó los 96 planes del 6-sep — *un corpus que vive en la base no es un corpus*. La foto del 6-sep se conserva como historia (no comparable). Hallazgo de paso: V7a/V7e disparan en 47 y 38 de 64 comidas (13 % el 09-07) — para CUL-P0-03, no para el medidor. **Corrección (`P1-PLAN-LOTE-22`)**: las 80 etiquetas humanas SÍ existen desde el 2026-09-07 (`a7d45ffe`, el dueño etiquetó a ciegas: 68 defecto / 9 ok / 3 dudoso) y el marcador binario ya da cifras — esta fila y la memoria del lote 18 decían que faltaban. Lo que falta es la anotación con RÚBRICA (C1) y un segundo anotador |
| C1 | ✅ 2026-09-15 · instrumento con la anotación del dueño y línea base estricta (`P1-PLAN-LOTE-60`) · 👤 2.º anotador | `P1-PLAN-LOTE-22`: CUL-P0-01 — el scan devuelve su ESTADO (`scanned/no_meals/no_catalog/error`, persistido en `_culinary_contract_scan` y propagado T1→T2), cada violación lleva `meal_index` (identidad por OCURRENCIA), el juez recibe `idx` y devuelve `meal_index` (`resolve_judge_violations` ata las quejas antiguas por franja sólo si es única: `declarada/unica/ambigua/sin_comida`), cada entrada del historial lleva `context` (rúbrica, modelo, país, esquema, reglas) junto al sello, y `judge_evaluation_state` distingue `juzgado_vigente/obsoleto/no_disponible/no_evaluado/desconocido` — sólo vigente y sin hallazgos aprueba. La línea base publica `estado_evaluacion`, `hallazgos` (atados / sin comida / ambiguos), `juez_entregado` (separado del histórico) y `particion` que RECONCILIA con el denominador; entran en `CIFRAS` y la base del corpus fijo se re-congeló y reproduce ×2. **Hallazgo al re-congelar**: los 5 planes salen «juzgado_obsoleto» (0/64 sobre lo entregado) porque el sello del plan lleva la posición del día y el shift archiva y renumera ⇒ sello por COMIDA (`meal_seal`, contenido) en cada violación, robusto al shift; las entradas viejas siguen sin poder decidirse. CUL-P0-02 — `culinary_golden_score.py --estricto`: adjudicación hallazgo↔defecto por `RUBRICA` (FP localizado + FN del esperado, duplicados no multiplican, cero división → null), acuerdo kappa + discrepancias, adjudicación, IC95 por conglomerado (plan), `--particiones` por linaje; `--plantilla`/`--ciego` preparan la anotación. Calibrador y test golden cruzan por COMIDA (en golden_05 un V1 del Desayuno contaba como acierto de la Cena). **Hallazgo**: las 80 etiquetas existen desde el 09-07 (binarias) — con ellas el estricto sale INCOMPLETO (exit 4) a propósito: faltan clase/severidad/evidencia por defecto y un 2º anotador; no se rellenan con el modelo · **14-sep**: anotación del dueño recibida (80/80: 66 defecto · 5 dudoso · 9 ok, 65 defectos con clase; anotación ciega), volcada a `docs/culinary_golden_anotaciones_angelo.json`; al puntuarla, `--estricto` dio 0 aciertos por el filtro de `alimento` del adjudicador (sin él: determinista 11/30/35) y las columnas `maquina_*` son del 09-06 — lote 38 del plan 38-43. Sigue 👤: 2.º anotador y el caso `060b4fda6a` · **15-sep** (`P1-PLAN-LOTE-60`, lote 38 del plan): el adjudicador empareja por código cuando el hallazgo no nombra alimento y por código+alimento cuando sí (el alimento acusado viaja en el texto de las columnas refrescadas), «defecto» sin defectos queda `sin_rubrica`; `scripts/culinary_golden_refresh.py` escribe `maquina_<capa>_2026-09-15` al lado de las del 09-06 (juez de producción, una comida por llamada, $0,0387 contados de $0,50, 0 escrituras en la base); línea base estricta en `culinary_baseline_estricto_2026-09-15.md`: determinista 11/30/36 → 18/60/29 (tp/fp/fn; recall 23,4 → 38,3 %, precisión 26,8 → 23,1 %), juez 2/32/7 → 0/28/9. `060b4fda6a` transcrito por delegación el 14-sep. Sigue 👤: el 2.º anotador (sin él, ni kappa ni promoción) |
| C2 | ✅ 2026-09-12 · contrato final de cantidades · knob `repair` | `P1-PLAN-LOTE-23` (CUL-P0-03): medido primero — en el corpus fijo 100 de 111 hallazgos de capa 1 son cantidades que pasos y lista se contradicen (V7a 47, V7e 38, V6 11, V4 4 de 64 comidas) y 54 de las 68 comidas con defecto humano lo dicen en la nota. Causa: la lista la mutan media docena de reparadores y los sincronizadores de pasos corren en puntos fijos — *una reparación que corre después del sincronizador deja lista y pasos desincronizados* (re-ejecutar el sincronizador viejo sobre el corpus corregía «casca 6 huevos» → 3: al persistir no corrió tras el cap). `recipe_contract.reconcile_step_quantities` (módulo nuevo, puro) reescribe en los pasos la cantidad que contradice a la lista, familia por familia, con guardas (reparto, gramatical, conteo de compra con gramos, rangos, ≈, notas); corre ÚLTIMO en los dos finalizadores del persist boundary tras `MEALFIT_RECIPE_FINAL_CONTRACT` (repair/shadow/off). V4 pasa a atribuir gramos por GRAMÁTICA (`grams_owner`): por cercanía los 265 g del tomate eran del nabo y el reparador los habría reescrito sobre el alimento equivocado. Corpus (copia): **V7e 38 → 3, V6 11 → 0, V4 4 → 0**, V7a 47 → 44 (lo gramatical se deja en warn), 63 reescrituras en 36 comidas, idempotente. God-file 53.099 → 53.062 (`_ground_meat_step_noun_sync` extraído). NO convierte el residuo en rechazo (guard sigue `warn`: la escalada necesita el marcador estricto de C1) y no construye un degradador de verbos para «técnicas del alimento anterior»: V1 en el corpus son 4 falsos positivos (aceite «a la plancha»), sin caso medido de residuo de sustitución **`P1-PLAN-LOTE-31` (13-sep)**: los 10 V7e «de fábrica» del bench real eran tres cosas — 7 piezas que cruzan el singular/plural con el paso pidiendo MÁS que la lista (el residuo `gramatical` de este contrato: ahora se reescribe con concordancia de número, artículo, sustantivo canónico con tildes, adjetivos y clíticos; la dirección «pide MENOS» sigue en `gramatical` por la decisión V7a del dueño), 3 sumas del detector dentro de un paso (V7e compara la mención mayor) y el LLM repitiéndose «y 6 claras de huevo y 6 claras de huevo» (paso (6) `colapsar_repeticiones`). Pareado real V7e 10 → 0 sin hallazgos nuevos; corpus 0 hallazgos nuevos en las 8 superficies; `insert`/`quality`/`swap`/`modify` **V7e 38 → 1** (antes 38 → 3: caen «las 2 tostadas de casabe» → «la tostada de casabe» y «2 plátanos verdes» → «½ plátano verde»; el que queda es «1½ tostones de casabe», declarado `gramatical` a propósito), resueltos 55 → 57, sellos 45 → 45 y V7a 42 → 42 — la otra dirección no se tocó, como se decidió |
| C3 | ✅ 2026-09-12 · las tres formas del huevo · 👤 compra de claras en envase | `P1-PLAN-LOTE-24` (CUL-P0-04): medido primero — nutrición, compra y el techo por pieza YA distinguían entero/clara/yema (catálogo: 33 g / 50 g / 17 g por unidad, `fdc_id` propios); la receta era la única capa que las confundía. En el corpus fijo 14 de 64 comidas llevan huevo, 6 salieron del tope diario de enteros (`_cap_daily_whole_eggs`: «6 huevos» → «3 huevos» + «3 claras» EN LA LISTA) y en las 6 los pasos seguían diciendo «casca 6 huevos»; el contrato de C2, ciego a la forma, lo reescribía a «casca 3» y las claras desaparecían de la preparación. **Hallazgo de orden**: el contrato de C2 NO era el último del persist boundary — detrás de `finalize_plan_data_coherence` siguen el band-closer, los caps de realismo, el tope de huevos, el piso de proteína y el re-cuadre de conteos (`db_plans._finalize_plan_data_for_insert`), y en swap y chat-modify el re-cuadre del día y el motor; ahora corre también en la COLA de los tres (`P1-PLAN-LOTE-24-FINAL-CONTRACT-TAIL`, idempotente). `recipe_contract.reconcile_meal`: (1) la lista nombra la forma («12 huevos sin yema» → «12 claras de huevo»; medido: heredaba 600 g y 57 g de grasa contra 396 g y 0,7 g) y los macros se re-miden; (2) los pasos siguen a la forma comprada («casca 6 huevos» → «casca 3 huevos y 2 claras de huevo»; «bate 1 huevo» → «bate 1 clara de huevo», «el huevo» → «la clara», nota de seguridad sin «yema firme»); nunca al revés (una lista de enteros con «hasta que la clara cuaje» es técnica). Corpus: **5 comidas contradictorias → 0**, 8 pasos, idempotente. El pedido de claras sobrevive a «enteros primero» y a los fallbacks: quien declara la Clara de huevo como básico recibe «HUEVOS: CLARAS PRIMERO» (`override_egg_form_preference`, sustitución de la regla, prompt byte-idéntico para el resto), el diversificador de pools no le quita el huevo básico, y la ración pedida llega al bloque de política con su forma. Knob `MEALFIT_EGG_STAPLE_HONORED`. Lector: `scripts/medir_formas_huevo.py [--vivo N]`. Ya cierto y ahora anclado por test: alergia a huevo cubre clara/yema; vegano rechaza las tres; el escáner de huevo crudo las ve; el ancla «Clara de huevo» NO se da por cumplida con huevos enteros. **Lo que NO hace, dicho**: la compra sigue colapsando claras en cartones de `Huevo` (decisión del 05-11: el usuario separa y aprovecha yemas) aunque el supermercado YA tiene «Clara de huevo · Don Papito · botella pasteurizada 400 g · RD$154,95» mapeada al catálogo — comprar claras en envase es una decisión de producto (👤) sobre el agregador de compras, no un dato que falte; el techo de 25 % de comidas con huevo y el «1 comida con huevo por día» del prompt siguen; honrar «10 claras» sigue bajo el canary de la política (E5 fase B) · **15-sep** (`P1-PLAN-LOTE-61`): la compra de claras en envase quedó decidida por delegación (botella a partir de 4 por comida) y MEDIDA antes de construirla — 1 de 64 comidas del corpus fijo la cruza (2 de 80 en el golden set); toca el agregador en las 6 superficies con el god-file a ~150 líneas del tope: su propio lote |
| C4 | ✅ 2026-09-12 · asignación paso↔ingrediente · ✅ 2026-09-15 las 3 recetas del dueño en la biblioteca, `revisar` 3 → 0 (`P1-PLAN-LOTE-61`) | `P1-PLAN-LOTE-25` (H8): medido primero — la biblioteca congelada (193 recetas, 867 pasos) no lleva cantidades de ingrediente a propósito, así que V6/V7a/V7e (que leen cifras) callaban sobre ella: materializadas como plato, 6 hallazgos (V1 4, V3 1, V5 1) y ninguno de cantidad; «tres mitades del aceite» pasaba limpio y «la mitad del cilantro» sin la otra mitad (la cuarta clase de las notas humanas, «usado a medias») no tenía detector. `recipe_usage` asigna a cada paso los constituyentes de SU plantilla (vocabulario cerrado: «el pollo» sólo puede ser la pechuga) con la FRACCIÓN de lo comprado, leyendo las pistas del texto en su cláusula («la mitad», «el resto», «… restante», «parte de», reservas y referencias que no consumen; una fija heredada por conjunción no parte al de al lado), y cuenta: Σ = 1 por constituyente, o dice exactamente por qué no (sin uso → V3, a medias → V7a, sobre-asignado → V6). Vive APARTE de la biblioteca y atada al texto por `pasos_hash`: la receta no cambia un byte, la firma curatorial NO caduca y una receta editada caduca su asignación sola. Resultado: **155 exactas, 35 estimadas, 3 a revisar; 1004 de 1017 constituyentes con Σ = 1** (los 13 restantes: 12 veces la `Sal` que ningún paso nombra —condimento— y 1 auyama); reproducible desde el texto y el catálogo del corpus. En el escáner, con asignación vigente V3/V6/V7a/V7e leen las cuentas en vez de adivinar (`P1-PLAN-LOTE-25-SCAN-EXACT`, la cadena de checks intacta; estado `exactas`); knob `MEALFIT_RECIPE_USAGE_EXACT`. Descartado medido: «fuera de plantilla» con el matcher global (50 acusaciones, «al sofrito» ×18 → Sofrito) — se reutiliza V5 (4). El coste previsto por la auditoría (190 recetas de revisión editorial + re-firma) queda en **3 recetas y ninguna firma**: «Yaniqueques horneados» (tres mitades del aceite), «Pollo al horno con batata y ensalada de repollo» («el resto del aceite» dos veces) y «Lentejas guisadas con auyama y batata» (la auyama no aparece en ningún paso) — su texto es del dueño. **Lo que NO hace, dicho**: no escribe cantidades en los pasos (C5), no toca las recetas del LLM («puede repartir» sigue siendo la regla sin texto congelado al que atarse), no escala a rechazo; en la flota hay 0 comidas de receta congelada en 30 días (día determinista en canario): hoy el valor es el inventario sobre la biblioteca |
| C5 | ✅ 2026-09-12 · 01/03/05 (`P1-PLAN-LOTE-26`), 02/04 (`P1-PLAN-LOTE-27`), 06/07 (`P1-PLAN-LOTE-28`), reparador de 04 (`P1-PLAN-LOTE-29`), benchmark en modo REAL ejecutado + paso (5) del contrato (`P1-PLAN-LOTE-30`) · 👤 rúbrica anotada para calibrar al juez | **`P1-PLAN-LOTE-30` (CUL-P1-07 en modo real)**: 3 planes recién generados (baseline, DM2, vegetariana; 3 días) por $0,054 de $0,50, sin escribir en la base (158 escrituras de telemetría suprimidas y contadas; `--telemetria-prod` las deja pasar), imitando a `/analyze` y con el coste contado en proceso. Hallazgo: la cadena del INSERT borró «15 g de Granola» (banda de grasa → piso de porciones) y el paso siguió nombrándola — V5 nacido en la cadena. Paso (5) del contrato: `recipe_repair.retirar_sin_lista` quita la mención del paso, verifica con V5 y se deshace si abre otro hallazgo (la primera versión abrió 3 V3 en el corpus: medido antes de la definitiva). Pareado real `--planes-de`: 1 → 0. Los sellos del contrato ya no se pierden al persistir (8 → 5 antes). **`P1-PLAN-LOTE-29` (reparador CUL-P1-04)**: `recipe_repair` adapta los tres defectos de V9 sin tocar la lista ni una cifra de compra (tortilla → salteado antes del huevo; wrap → lo que cierra + el resto al lado; crema → sólo el líquido que espesa + el resto como bebida), idempotente (V9 deja de acusar), paso (4) del contrato final bajo `MEALFIT_RECIPE_FINAL_CONTRACT` con telemetría `estructura`. El paso degradado del camino sin LLM conserva los demás alimentos y cuece lo seco. Corrección: el bench del lote 28 degradaba por cualquier check (el cron sólo por V1/V2) — re-medido: 1 hallazgo nuevo, no 21; con el reparador, 0. Sin casos de V9 en corpus ni biblioteca: medido sobre los casos del backlog. **`P1-PLAN-LOTE-28` (06, 07)**: el juez gana `componente`, `intencion` y `certeza` (segura|dudosa) con defaults compatibles; una dudosa se OBSERVA y no bloquea (sólo las seguras deciden `blocked`); esquema de hallazgo `2026-09-12.certeza`; el marcador estricto excluye `[dudosa]` salvo `--con-dudosas` y `--excluir-dev` saca los casos de desarrollo del holdout. `scripts/bench_superficies_culinarias.py`: 8 superficies medidas con las mismas fotos (`repair_stage_diff`), artefacto con git sha + huella de reglas + catálogo, `--informe` offline, `--comparar` pareado, `--real` que se niega sin presupuesto, nada escribe planes. Medido sobre el corpus con el catálogo real: insert/quality/swap/modify resuelven 54 y estampan el mismo contrato en 20 comidas (**la receta de la semana 3 y la del swap tienen el mismo contrato**), 0 hallazgos nuevos; el postfix del día DEGRADADO introduce 21 (V3 ×15, V7c ×5: quita el paso y deja el alimento comprado sin uso) — hallazgo real del camino sin LLM, anotado. `/recipe/expand` no tiene cadena determinista (se dice). No hecho: calibrar al juez (LLM + rúbrica anotada del dueño, C1/C6), interfaz/PDF contra snapshots. **`P1-PLAN-LOTE-27` (02, 04)**: `dish_structure` — familia, componentes (sólo lo que la lista declara en g/ml) y tres relaciones con umbrales sacados de la biblioteca curada (wrap relleno/pan 2,4–3,8 ⇒ tope 5,0; crema sólidos/ml mediana 0,90 ⇒ menos de 0,20 sin proceso ni espesante es la crema del backlog; 12 de 14 tortillas curadas preparan el vegetal antes del huevo) como **V9** (warn): 0 falsos positivos sobre 193 recetas y el corpus, los 3 casos del backlog disparan; 48 de 64 comidas del corpus con confianza baja (lista en tazas y piezas — se dice). `repair_stage_diff` — la cadena de reparación se fotografía a la entrada, tras los caps y a la salida del persist boundary y escribe en `_repair_stage_diff` qué hallazgos introdujo cada etapa (knob `MEALFIT_REPAIR_STAGE_DIFF`); lo ya cierto quedó escrito: los cerradores re-escalan porciones existentes (no añaden alimentos), la sustitución de huevo reescribe pasos, C2/C3 cierran la cola. No hecho: adaptar relleno/técnica (V9 acusa), medición sobre planes reales (llega con la telemetría; red caída al escribirlo). `P1-PLAN-LOTE-26`: medido primero — claras entregadas 1-5 por comida, ningún plan declara la clara como básico, 5 de 6 planes ya con la política EN VIGOR (el tope por ración de E5 está activo); 5 de 64 comidas del corpus declaran 15-20 min y piden horas (4 son conservación); el equipo sólo lo leía el prompt del plan (panel opt-in, 53 de 193 recetas congeladas piden horno); la rúbrica del juez decía «arroz/pasta NUNCA de noche» y los países beta la heredaban. `culinary_context.py` (única lectura de lo declarado; knob `MEALFIT_CULINARY_CONTEXT`): **CUL-P1-03** — lo típico, lo preferido y lo prohibido son tres cosas: un básico declarado para una franja exime la regla de horario en el gate y en el autofix del arroz de noche; la rúbrica pasa a «REGLA DE HORARIO — CONTEXTO, NO DOGMA» (pasta de noche legítima; avena salada/yogur como salsa/pescado con fruta por técnica; componentes separados no son mezcla) y los beta reciben la versión neutra; el juez recibe `contexto` (básicos, cocinas, equipo, tiempo). **CUL-P1-01** — el techo de comidas con huevo honra al huevo básico (≥ 1/día) y una clara de aglutinante no cuenta; con la política en sombra la ración no honrada queda ESCRITA en `relaxations` (`portion_cap_default_not_enforced`, deferred). **CUL-P1-05** — V8a tiempo oculto y V8b equipo no disponible (warn; sin declaración se dice `no_declarado`); el equipo al prompt del día, al selector determinista (poda) y a la métrica de personalización (`equipment_unavailable`); almacenaje ≠ consumo para los checks de cantidad; V7d compara ml con ml y cruza familias sólo con densidad respaldada. God-file 53.073 → 53.091 tras extraer el pareo chocante a `culinary_context`. Decisión de producto registrada en `culinary_coherence.md`. **Lo que NO hace**: no pregunta el equipo en el formulario principal (decisión vigente), no rechaza por V8, no toca el arroz DURO del desayuno, no reparte claras por receta ni calibra al juez (segunda parte) · **`P1-PLAN-LOTE-62` (lote 39 del plan · CUL-P1-05: lo que el dueño más marcó)**: V7f `coccion_faltante` (warn, knob `MEALFIT_CULINARY_V7F`) y la RUBRICA gana su código; V7c lee «crudo» y no da por cocida una legumbre seca con «cocina 3 minutos»; V5 mira la frase del alimento. Estricto contra la línea base del lote 38: `coccion_faltante` 0/8 → 8/8, `seco_sin_coccion` 3/11 → 11/11, `usa_lo_que_no_esta` 1/9 → 2/9; determinista 18/60/37 → 35/65/20 (recall 32,7 → 63,6 %, precisión 23,1 → 35,0 %); 0 hallazgos sobre los 9 `ok`. 👤 dos desayunos de la biblioteca sirven «el huevo revuelto» sin paso que lo revuelva: añadirlo es de su redacción |
| D4 | ✅ 2026-09-12 · sin barrido | `P1-PLAN-LOTE-17`: el `30` lo escribe la rama «polvo de queso» del piso (`_floor_subservible_portions`) vía `rescale_ingredient_string` con factor 15 g / gramos del polvo (60× para 0,25 g, 120× para 0,125 g) sobre la línea de raw elegida por ÍNDICE; conserva el token de unidad, y el `30` es 15 × 2 (la cantidad de raw era el doble de los gramos del polvo en los tres casos). REPRODUCIDO con la función real: con `RAW_PAIR_BY_FOOD=False` salen `30 cdas de cebolla picada` y `30 cdas de aceite de oliva` tal cual. La rama ahora sólo escribe raw si la línea es de queso (dejaba de depender del knob). Residuo medido en la flota actual: 6 planes, 100 comidas, **0** líneas absurdas — los 3 planes del hallazgo ya no existen (purga) ⇒ **nada que barrer**. Sonda repetible: `scripts/medir_cantidades_absurdas_raw.py` (sólo SELECTs; compara raw↔display en ml, sin el falso positivo de «2¼ tazas = 34,75 cdas») |
| A9 | ✅ 2026-09-12 · curado por el DUEÑO | `P1-PLAN-LOTE-16`: los 28 platos pendientes de juicio humano (19 DO de P1-CATALOGO-PROTEINA-DESAYUNO, 9 ES de E9) juzgados plato a plato en la ficha interactiva (https://claude.ai/code/artifact/d9829853-4e63-469b-b20a-7db31a736d50): **28/28 «con cambios», aplicados**. 2 renombres con id estable («Huevo duro con aguacate», «Coditos con salsa de tomate y queso gouda» — el catálogo no tiene queso curado); 16 técnicas corregidas con id estable — la técnica entra en `mint_template_id` igual que el nombre, de ahí `plan_policy.TEMPLATE_MINT_TECHNIQUE`; `spec` por constituyente (estado en que se pesa / cómo se usa) y `prep_notes` por plantilla ES, que el compilador ahora deja pasar al snapshot sin mover nombre ni nutrición; las 19 recetas DO reescritas al pie de la letra (biblioteca v8). Medido contra el árbol firmado: 19 + 9 plantillas tocadas, 0 ids perdidos, 0 cambios de nutrición, 0 de estado; firmas DO/ES re-ancladas, benchmark y baseline C3 regenerados. **Lo que NO se aplicó tal cual, dicho**: «aguanta N d»/«solo despensa» eran la etiqueta de MI ficha para `logistics.days_fresh_min` (vida de los INGREDIENTES crudos, la pregunta del ciclo de una compra), no la conservación del plato hecho — 28 de 28 veredictos lo señalaron; se corrige la etiqueta, no el dato. Y «recalcular con el catálogo» ya era el cálculo: ninguna fila cambió |
| E4 | ✅ 2026-09-12 · 🧭 canario vivo | `P1-PLAN-LOTE-15`: la parte de ARQ27-P1-06 que la batería declaraba abierta. Medido ANTES de tocar código: el coste LLM no se podía atribuir a NINGÚN plan de la cola (`day_generator` 0 de 116 filas con `plan_id`, 0 con `corr`; `swap_meal` 0 de 117 con `user_id`) porque el canje `corr → plan_id` de P1-COST-ATTRIBUTION nació para el SSE (id DESPUÉS de generar) y en la cola el id ya existe ANTES. `plan_id_var` + `set_llm_attribution` en el worker de chunks (reset en el finally), `/swap-meal` (sólo el plan PROPIO) y `/regenerate-day`. `scripts/canary_plan_delivery.py`: fallos, reintentos, latencia del bloque 1 (p50 278 s, p95 361 s en 6 planes), coste y swaps por plan con denominador por cohorte («sin atribuir» ≠ US$ 0). `scripts/verify_swap_last_chunk.py`: recetas↔lista con el MISMO guard de producción (emit anulado en el proceso), recetas completas, horizonte y proyección sobre los planes persistidos — **6/6 coherentes, 0 severas**, incluido el plan del dueño con 48 revisiones de swaps. Hallazgo para el dueño: la proyección de compras sale `stale` en 6/6 porque compara revisión (que sube con cualquier escritura) y no la huella de la lista. Doc `docs/canary_entrega_e4.md` |
| E2 | ✅ 2026-09-12 | `P1-PLAN-LOTE-14`: **el flip ya estaba vivo** — `MEALFIT_INITIAL_VIA_QUEUE=true` en el `.env` del VPS y `VITE_INITIAL_VIA_QUEUE=true` en el frontend (leídos en el VPS el 09-12; `prod_profile.py` ya lo traía el 09-06); 6/6 planes nuevos desde el 09-04 nacieron por la cola (2 usuarios, 0 legacy); 0 alertas del lifecycle desde el canary del 09-02 (las 15 de ese tramo son de calidad, i18n, `plan_jobs_dead` o `country_beta_first_plan`); gate del 09-02: 10/10 planes, 2/2 kills (+1 real). Lo que faltaba era ANOTARLO. Hallazgo: `/admin/arq25-gate` decía `ready_to_flip: false` (runs 6, kills 0) porque la purga de cuentas del 09-11 se llevó en CASCADE las filas del canario (usuario f47126cb) — *un gate que vive en filas que una purga borra no recuerda que pasó*; ahora responde `phase`/`flip_live`/`counts_scope` sin depender de ellas |
| D8 | ✅ 2026-09-14 · decidido: NO construir `shopping_commercial` (delegado, `decisiones_dueno_2026_09_14.md`) | `P1-PLAN-LOTE-14`: `scripts/measure_plan_jobs_lag.py` (read-only, repetible). 30 días: 35 jobs, 0 `dead`, 0 backlog. **Recogida del worker p95: 101,6 s (display_i18n) · 9,4 s (shopping_projection)** — dentro de los 2 min del gate de la Fase 5; consumo p95 54 s / 1,7 s. El p95 «total» de 2.450 s de display_i18n NO es el worker: 1 job del incidente del 09-08 (`already_enriched` ×5 → dead → revivido, 8.552 s; cerrado por `P1-I18N-DEAD-VEREDICTO`) y 4 cadenas `revision_changed` re-encoladas por el reconcile (20 min) mientras el dueño probaba swaps (plan 3957a669, revisión 26). 9 de 14 jobs limpios eran no-ops de 0 s: contados aparte, porque metidos en el p95 lo escondían. `shopping_commercial` no se construye: ningún consumidor la pide (el precio y el envase ya salen de `supermarket_products`); decisión de producto del dueño |
| E9 | ✅ 2026-09-12 | `P1-PLAN-LOTE-13`: medido con `scripts/coverage_funnel.py` ANTES de escribir un plato: de los 10 almuerzos vegetarianos de ES, 9 llevaban calabacín/berenjena/tomate/pimiento/espinacas/acelgas (7-10 días) y sólo la tortilla aguantaba el día 25. La regla de conservación dice la verdad; lo que faltaba eran almuerzos de DESPENSA. +9 plantillas ES (legumbre seca, arroz/pasta, huevo, patata, zanahoria, col, cebolla/ajo, conserva; todo ≥ 35 días; 375-672 kcal, 18-26 g proteína, sodio < 600): **almuerzo 1 → 10, cena 3 → 8** en «veg · día 25 sin congelador»; vegetariano 10 → 19 y vegano 7 → 10 en almuerzo; la ⚠ del embudo desaparece. Registry ES 133/133 `ok`; firma re-anclada y las 9 anotadas en `pendiente_de_juicio_humano` (A9, dueño) |
| Residuos técnicos | ✅ 2026-09-12 | `P1-PLAN-LOTE-13`: (1) los **3 tests dependientes del orden** tenían UNA causa medida —`test_p1_form_efecto` escribía `os.environ["MEALFIT_COUNTRY_SYSTEM"]="true"` sin restaurar y hacía `reload(constants)`; con el knob filtrado la ruta USD del piso cambia de fórmula, «tortilla de maíz» resuelve a su fila y la identidad `is` del SSOT renal muere—; reproducido en un proceso (3 failed) y verde tras `monkeypatch.setenv` (0 failed); fuera los 3 `reload(constants)` que quedaban; dos ratchets. (2) Las **5 bibliotecas en DRIFT de catálogo** desde el 09-09: recompiladas y re-ancladas con lo MEDIDO plato a plato (MX/PR/US cero cambios; CO un dato derivado: el fósforo del Borojó dejó de ser desconocido); `--check` OK en las 6. (3) El **generador** `build_dish_constituents_do.py` (75 entradas divergían) retirado; `scripts/check_dish_constituents_do.py` valida tabla↔plantillas. (4) Los `COALESCE(...,0)` del **refresh nocturno** y su helper caen al SSOT `DEFAULT_TZ_OFFSET_MIN` (0 explícito sigue siendo UTC). (5) Los **7 pasos del wizard sin campo** llevan `id` propio (frontend `840b236`→) y `step_id` lo prefiere. Quedan: `PYTHONHASHSEED`, el knob `W_CARB_SURPLUS=2` (dueño), `10k-websites/` (dueño) |
| A1 | ✅ 2026-09-11 | main `3a783da` mergeado y desplegado; `drift=false`; el canario (`MEALFIT_DETERMINISTIC_DAY_USERS`) come ya con el esqueleto real |
| A3, A4, A5, A6, A7, A8 | ✅ 2026-09-11 | `P1-PLAN-FASE-A` (`tests/test_p1_plan_fase_a.py`). A7: la costura `apply_library_recipe` **no tiene llamadores en producción** (0 de 60 comidas LLM de 14 días casan por nombre); queda correcta para cuando se cablee |
| A2 | ⏳ bloqueado | falta `BioBoros_auditoria_arquitectura.md` |
| A9 | 👤 dueño | re-firma curatorial |
| E1 | ✅ parcial 2026-09-11 | los 445 «modificados» del frontend y los 6 de la raíz eran FANTASMAS de `core.autocrlf=true` contra `.gitattributes eol=lf` (`git add --renormalize` los limpió sin cambiar un byte; `autocrlf=false` en los 3 repos). Lo real se commiteó (`49a136e` raíz). Queda: tag `baseline-v1`, retirar worktrees viejos, `10k-websites/` sin dueño en la raíz |
| F1 | ✅ ya estaba | la CHECK `user_profiles_country_supported` y su índice EXISTEN en Neon y el libro tiene su fila (el doc del 08-23 quedó viejo) |
| F4 | 👤 dueño | secreto `SIBLING_REPO_TOKEN` (o deploy key, ver mensaje) — el repo backend sólo tiene `ZAI_API_KEY` |
| CI | ✅ 2026-09-11 | `P0-CI-TIMEOUT-15` (`ff0162a`): TODAS las corridas desde el 09-09 morían a los 15 min; ahora xdist + cuarentena + 50 min |
| B6 | ✅ 2026-09-11 | `P1-PLAN-LOTE-2`: memoria entre días (`memoria=` compartida por el run + días persistidos del bloque anterior); los saturados van al final. Falta medir sobre el canario si las 9 ventanas rotas bajan; después decidir `MEALFIT_DETERMINISTIC_DAY_SAME_DAY_VARIETY` (B8) **`P1-PLAN-LOTE-36` (13-sep)**: el instrumento que faltaba, `scripts/measure_variety_windows.py` (solo lectura, conteo de `_conteo_ventana`, tope de la política de cada plan). 6 planes en 60 días; antes del corte, ninguno con día determinista y ≥ 7 días (el plan de 30 días del dueño que dio las 9 ventanas ya no está en la base); del modelo, 0 rotas de 7 ventanas. Después: SIN MUESTRA: ningún plan posterior al 2026-09-11 con día determinista y ≥ 7 días entregados. En el banco de 30 días del lote 35, CON memoria, 14 de 24 ventanas siguen rotas por dos plantillas sin con qué rotar. Volver a medir cuando el canario entregue una semana |
| D1, D2, D3 | ✅ 2026-09-11 | `P1-PLAN-LOTE-2`: el sobre pesa `density_g_per_unit` (5 g) y no la caja (40 g); «Cebolla en polvo» y todo polvo/deshidratado con fila propia deja de colapsar al fresco; el tope de condimentos reescribe `display_string`. Hallazgo colateral (sin tocar): el chain también colapsa «Plátano verde»/«Plátano maduro» → «Plátano» y «Guineo verde» → «Guineo» (13 filas pre-existentes anotadas en `test_p1_country_system_f2`) — para un mangú no da igual |
| G2 | ✅ 2026-09-11 | `P1-PLAN-LOTE-2`: `get_user_tier` lee 'free' como 'gratis'; migración `p1_plan_tier_gratis` (UPDATE + DEFAULT + CHECK). Los 6 usuarios e2e y el `plus` sin suscripción ya no existen (2 perfiles vivos, ambos 'free' → 'gratis') |
| G3 | ✅ 2026-09-11 | `P1-PLAN-LOTE-2`: la purga con `delete_auth_user=true` borra `neon_auth."user"` (cascade a `session`/`account`) y olvida el positivo cacheado del guard P1-AUTH-CUENTA-BORRADA |
| F7 | ✅ parcial | `p1_fosforo_borojo_hoja_santa`: Borojó 160 mg y Hoja santa 38 mg con fuente citada. **Achiote, Chontaduro y Champús siguen en NULL a sabiendas**: la TCAC 2018 del ICBF es un PDF escaneado sin texto; si el dueño tiene la tabla en Excel, se completan en 5 min |
| F10 | ✅ 2026-09-11 | el marker `P1-COUNTRY-CAPS-DO-LEXICON` ya está en `MEMORY.md` |
| B5 | ✅ 2026-09-11 | `P1-PLAN-LOTE-3`: perfil `neutral` («Sin cocina en particular», chip sin bandera, 4 idiomas), `diet.exclusions` → `template_candidates(exclude_foods=)` desde blueprint, prompt y día determinista, `culture_unavailable` anotado en el blueprint y publicado por el informe de fidelidad |
| B1 | ✅ parcial | `P1-PLAN-LOTE-3`: el informe declara `checks_run`/`unmeasured`/`n_checks`; mide `culture_share_*` (≥2 cocinas, ≥4 platos identificados), `prep_time_over_budget` (`cookingTime` none/30min/1hour) y `anchor_portion_*` (anclas en piezas). `equipment_unavailable` sigue sin medirse: el formulario no pregunta el equipo (decisión de producto) |
| B3 | ✅ | `P1-PLAN-LOTE-3`: `computation` en `_fidelity_report` + `computation_hash` en la métrica (registry, generación del catálogo, hash del prompt del día, modelo, semilla, marker del código). Separada de `input_hash`; sin columna nueva |
| D5 | ✅ 2026-09-11 | `P1-PLAN-LOTE-4`: umbral de cantidad en el gate de reservas (`reservation_line_is_material`, knob `MEALFIT_PANTRY_RESERVE_MIN_G`=15) con la misma vara en las dos orillas, y `malla` = 5 lb. Las «4 lecturas sueltas del flag» siguen cubiertas por `test_p1_pantry_gate_ssot` |
| CI | ✅ verde | primera corrida VERDE de la CI del backend en GitHub: `P1-PLAN-LOTE-2 (2/2)` (2026-09-11 16:44). Dependientes del orden bajo xdist (pasan aislados; F5): `test_p3_condition_rules::test_detector_terms_are_ssot_shared`, `test_p1_prep_collapse_guard::test_distinct_preparations_do_not_collapse` («Tortilla de maíz» YA tiene fila en el catálogo MX: el test asume que no), `test_p1_budget_floor_personalized::test_endpoint_usd_conversion` (tasa USD cacheada por otro test), `test_p1_arq27_f3_bateria` ×2 (otro test deja `MEALFIT_COUNTRY_SYSTEM` puesto en `os.environ`) |
| G52 | ✅ | `P1-PLAN-LOTE-8`: el bloque temporal «OBLIGATORIO» calculaba «hoy» con el reloj del SERVIDOR (viernes 21:00 en Santo Domingo ⇒ «Sábado · FIN DE SEMANA»; pegaba también a DO). `build_time_context(country, tz_offset_min)` recibe el offset del usuario desde el contexto compartido vía `constants.tz_offset_min_for_form_data` (`tzOffset`/`tz_offset_minutes`; 0 explícito es UTC; sin dato el SSOT `DEFAULT_TZ_OFFSET_MIN`, nunca el default por país — T5-F1). Sin offset, byte-idéntico a antes |
| G53+G74 | ✅ | `P1-PLAN-LOTE-7`: la 2.ª tabla clínica (`condition_rules._BETA_CLINICAL_FOOD_SWAPS`, 10 filas, divergida del SSOT desde el día en que nació) desaparece; `build_condition_prompt` neutraliza con `constants.neutralize_do_lexicon` y sus 3 frases largas pasaron al SSOT (primero, orden largo→corto). Medido en ES bariátrico: desaparecen «lechosa» y «víveres» (G74); quedan guineo, batata, habichuela, chicharrón, criollo y tostones — identificadores del catálogo o léxico que el SSOT excluye a sabiendas (guineo→plátano es INSEGURO: son dos filas distintas). DO sigue byte-idéntico |
| D7 | ✅ medido | Serie diaria del cron `_shopping_coherence_alert_job_tick` (30 días con tick, hasta el 09-11): **3 `magnitude_undersupply` en total** (1 el 09-04, 2 el 09-05) sobre 68 planes-con-divergencia, concentrados en la semana de ráfaga del 09-03/09-06 (31 planes con divergencia el 09-03: canario y pruebas, no tráfico orgánico); 0 en los otros 27 días. En `meal_plans` (60 d) sólo 6 planes conservan history (20 entries, 0 undersupply). Volumen «unidades, no decenas» ✔, pero muestra minúscula y en ráfaga ⇒ **recomiendo NO encender `MEALFIT_GUARD_UNDERSUPPLY_SEVERE` todavía** (un severo en falso = dead-letter del chunk por reintento idéntico) y re-medir con un mes de tráfico orgánico. Script read-only repetible: `scripts/measure_undersupply_volume.py` **`P1-PLAN-LOTE-36` (13-sep)**: re-medido a 45 días — los mismos 3 `magnitude_undersupply` (09-04 y 09-05, ráfaga del canario), 0 desde entonces; history viva en 6 planes, 0 undersupply. Sigue sin muestra orgánica: volver el 2026-10-10 |
| F5 | ✅ auditoría + 1.ª tanda | Informe `docs/audits/f5_guards_inertes_2026_09_11.md` (84 funciones sin llamador, 4 knobs sin lector, 153 flags `_x` sin lector, 91 guards que se tragan su fallo). `P1-PLAN-LOTE-5` cierra: escáner de alérgenos del pool fail-SECURE, notas de embarazo/condición y clases de alérgeno avisan cuando no se evalúan, 4 funciones muertas del god-file fuera, `close_connection_pool` fuera, el comentario del bypass dice la verdad. `P1-PLAN-LOTE-6` (2.ª tanda): los 68 manejadores `except: pass/continue/return` de las funciones del informe (§4.1-4.3) registran el paso que se tragan (`[P1-PLAN-LOTE-6]`, `info` dentro de bucles; en la cadena de `finalize_plan_data_coherence` además viaja `<paso>=ERR` en `parts`), los 4 veredictos advisory del intento final tienen LECTOR (alerta `plan_quality_degraded` con `final_attempt_advisories`, mismo knob que `approved_with_residual`; el banner del usuario no cambia: sus 14 motivos son contrato con el frontend), fuera `COUNTRY_SYSTEM_ENABLED`, `CHUNK_STALE_FINAL_LIVE_TIMEOUT_SECONDS` y `SLOT_AWARE_DAY_REPAIR` (sin rama), fuera los 2 `if True:`, y los 20 knobs default-off sin documentar están en `knobs_reference.md` con criterio de activación (2 marcados needs-owner). **Falso positivo de la auditoría, corregido en el informe**: la «cota absoluta de la cookie sin llamadores» SÍ se aplica en cada path de auth dentro de `_decode_session_cookie` (test `test_verify_rejects_past_absolute_cap`); el helper huérfano es sólo de tests. **Decidido → `P1-PLAN-LOTE-7`**: `MEALFIT_HARDEN_SAMEDAY_PROTEIN` (`true` en prod, sin rama) QUITADO del knob y de `prod_profile`; **para el dueño**: `_is_inventory_live_degraded` y `_refill_emergency_backup_plan` sin cablear; 5 veredictos advisory del intento final sin lector; ~20 pasadas de `finalize_plan_data_coherence` con `except: pass` |
| G52-b (hermano) | ✅ | `P1-PLAN-LOTE-9`: el estampado de `date`/`day_name` del plan (principal y días sintéticos), el techo y el ancla de `execute_after` del chunk, el drift/resync del learning gate (y su re-lectura bajo el lock), el drift del refresh de nevera y el persist del snapshot resolvían el huso con `... or 0`: sin dato asumían UTC (el SSOT dice RD) y el drift confundía «ausente» con «UTC» (un perfil vivo en 240 disparaba «viaje», con resync + push). Los 8 usan `constants.tz_offset_min_for_form_data`/`DEFAULT_TZ_OFFSET_MIN`; un ratchet por regex impide que la forma vuelva. Medido: 0 filas vivas sin huso hoy (2/2 perfiles en 240; 28 canceladas y 2 completadas sin huso, del camino viejo). Residuo documentado: los `COALESCE(..., 0)` en SQL del refresh nocturno (deciden la HORA del cron, no una fecha del usuario) |
| B7 | ✅ mitad técnica · peso por usuario del canario, inerte: medido en su perfil (ganancia), 2.0 empeora C y G (`P1-PLAN-LOTE-61`) · 👤 composición | `P1-PLAN-LOTE-10`: re-medido con `scripts/measure_deterministic_day_macros.py` (14 d, 2000 kcal, 150/200/60). La proteína ya estaba cerrada (+2,4 %, 14/14) por el catálogo del 09-09; el sesgo real era carbohidrato +18 % / grasa −17,7 %, en almuerzo y desayuno. El scorer gana pesos direccionales (knobs `MEALFIT_DETERMINISTIC_DAY_W_CARB_SURPLUS`/`_W_FAT_DEFICIT`, default 1.0/1.0 = conducta anterior). Medido en TRES dianas (matriz de 6 pesos + 4 formas simétricas): ninguna domina; 2.0/1.0 recorta el carbohidrato 6-7 pts y arregla la grasa a costa de ~2 pts de proteína en pérdida y 2-4 platos distintos — proteína y variedad contra carbohidrato es decisión de producto ⇒ **el dueño enciende `..._W_CARB_SURPLUS=2` en su canario si lo quiere**. 2.0/2.0 (perfecto en UNA diana) hundía la proteína en pérdida a −12 %. Lo que queda es composición (almuerzo: 2 platos ≤ 70 g de carbohidrato entre 18 candidatos) — dueño |
| B9 | 📏 medido · sin línea base posible | `pipeline_metrics.wizard_funnel`: 121 filas, 5 sesiones, 2 días con datos (el dueño). El embudo por paso del doc era CIEGO: el frontend nunca emitía `step_done` (0 de 121) aunque el backend lo aceptaba — **corregido en el frontend** (`840b236`: `step_done` al avanzar). SQL proxy (paso terminado = la sesión ve uno posterior) escrito en `plan_policy_f4.md`; 9 pasos sin `step_id` salen como `step_<n>` (frontend, dueño). La condición «≥ 2 semanas» se cuenta desde el lanzamiento |
| D6 | ✅ verificado | El «bug latente» de la rama `MEALFIT_TRIP_WINDOWED_PERISHABLES` ya estaba cerrado (la 2.ª pasada recibe `text_demand_g_map`, ronda de revisión de `P1-VEG-BACKFILL-HONESTY`); anotado en `knobs_reference.md`. Encender el knob sigue siendo decisión del dueño (4 prerrequisitos) |
| F3 | 📏 mitad SQL | De los 6 planes con comidas entre los 50 más recientes, **1** lleva `_display` (fr-FR, 4 de 60 comidas; el perfil fr-FR es el del dueño); `plan_jobs.display_i18n`: 10 `done`, 3 `stale`. La corrida real por idioma gasta LLM y el recorrido en la app nativa es humano — dueño |
| E3 | 🧭 evaluado | ARQ27-P2-01 es higiene del propio paquete de auditoría (fecha/SHA/denominador/script por cifra). El `README.md` del paquete ya trae SHAs (7b6df93 · 8d83abb · 82183e9), scripts y `manifest.json`; lo que falta es datar cada cifra de `ARQUITECTURA-2.7-3.0.md`. Propuesta: cerrar como cubierto por «Procedencia» + manifest — decisión del dueño |
| C8 | ✅ 2026-09-11 · curado por el DUEÑO | `P1-PLAN-LOTE-11`: las 4 plantillas DO sin receta congelada, juzgadas plato a plato en la ficha interactiva (https://claude.ai/code/artifact/0918e0a8-ab5f-4789-b83a-39edc3663023): 3 «con cambios» → renombradas a lo que traen («Frutas picadas con limón», «Mangú con jamón de pavo a la plancha», «Filete de pescado blanco al horno con vegetales y batata»), id estable vía `TEMPLATE_ALIASES`, recetas en `recipe_library_do_v1.json` v7 con sus cambios; 1 ❌ («Batida de zapote ligera»: sin zapote en el catálogo) retirada del registry. DO: 193/193 `ok`, 0 `partial`; firma curatorial re-anclada, benchmark y baseline C3 regenerados. El rótulo «Jugo de limón 10 g» se aplicó en `P1-PLAN-LOTE-12` (el catálogo del día resuelve alias con el MISMO resolutor que el compilador; antes sólo por nombre exacto). Lista y Nevera ya lo resolvían: `_parse_quantity` canonicaliza el alias y aplica el rendimiento del jugo (×2,86) — medido antes de añadir maquinaria. Hallazgo: `dish_constituents_do.json` es el SSOT, su script ya no lo reproduce (75 entradas) |
| E5, E6, E7 | 🧭 diseño + plan de canario escritos | `docs/arq30_e5_e7_diseno_canario.md` (`P1-PLAN-LOTE-10`): E5 en 4 fases (sombra → cohorte → promoción → retirada) con criterios de salida medibles; E6 con orden P1-03 sombra → P1-04 → P1-05 y las trampas ya pagadas; E7 con el primer paso barato (medición) por gap. Mover autoridad es decisión del dueño (cohorte y orden) |
| G4 | 🧭 evaluado (backend) · 👤 dueño | `ci/backend-layout-fix` (4 commits, último 2026-08-18): reescribe `ci.yml` (361 líneas) y añade `tests/ci_excluded.txt`; main rehizo la CI el 09-11 (`P0-CI-TIMEOUT-15` + QUARANTINE) y el gate corre — **superada, borrar**. `paises-ola-2` (1 commit, `P1-COUNTRY-CLOCKS` 08-21): main ya trae `test_p1_nudge_tz_per_user.py` con ese marker — **superada, borrar**. `pr8` (1 commit 08-07, «E2E real contra Postgres: la Nevera baja y vuelve a subir»): un test E2E que main no tiene tal cual; vale revisarlo antes de borrar (5 min del dueño). Frontend: `worktree-perf-frontend-fluidez` y `wip/paused-banner-no-flash` siguen siendo del dueño |
| F2 | ✅ reconciliado | `docs/superpowers/plans/2026-09-11-paises-gaps-reconciliacion.md`: de los 96 gaps, **63 cerrados** con evidencia (el doc anotaba 3), 3 parciales (G13, G41, G82), 26 abiertos, 4 no verificables. Los que más importan: G52 (reloj del servidor, afecta a DO), G53+G74 (segunda tabla clínica divergente del SSOT), G59 (el propio código lo confiesa) |
| F8 | ✅ medido | suite bajo los 6 knobs de producción: **104 fallos / 24.270**; atribución por knob sobre los 5 ficheros peores: `MEALFIT_VERIFIED_INGREDIENTS_ONLY=true` explica 56/56, los otros 5 knobs 0/0. Son harnesses con ingredientes sintéticos que el filtro de verificados descarta. Propuesta: paso del gate con `prod_profile` sobre un subconjunto curado + la batería; los 104 no se «arreglan» a ciegas **`P1-PLAN-LOTE-33` (13-sep)**: el paso existe — `scripts/prod_profile_gate.py` corre la suite con el perfil de producción exportado menos `tests/prod_profile_excluded.txt` (34 ficheros, cada uno con el knob que lo explica: re-medido hoy 109 fallos / 24819 tests; 30 por un knob, 4 por combinación, 0 sólo en la corrida completa) y después la batería; segunda pata de la CI en paralelo y tercera fase del gate local. Doc: `knobs_reference.md` §F8 **CI de GitHub (13-sep)**: medido al engancharlo — las 9 corridas desde el lote 26 morían `cancelled` a ~50 min (paso de pytest en 49,4 min) y las dos anteriores que terminaron fallaban por 3 tests que leían el workspace raíz; `timeout-minutes` 50 → 90 con 2 workers (`-n 4` murió a los 90 s y `-n 3` a los 17 min con el runner apagado: la memoria del runner es el límite), y los 3 tests separan su lectura del workspace en un test que se salta con motivo **bis 4 (13-sep)**: la pata `produccion` de la CI dio 76 fallos en 19 ficheros sin base (el gate local la tiene): lista aparte que sólo aplica sin base, medida en la propia CI (run 34781436422); `--durations=30` y techo 120 min **`P1-PLAN-LOTE-37` (13-sep)**: la cola lenta era Sentry sin DSN serializando cada error de log (7 setups de 630-750 s en `test_p1_arq27_f3_candidateset.py`); sin DSN, sin integraciones, y la suite sin DSN — local 478 s → 11 s; CI 88/87 → 10,5/8,3 min · **(bis, 14-sep)**: techo 120 → 30 min |
| B4 | ✅ | `P1-PLAN-LOTE-3`: semilla del run (`horizon.run_seed`, knob `MEALFIT_SEED_FROM_RUN`) en el prompt del esqueleto/día y en el sembrador (`random.Random` local). Queda para el dueño: `PYTHONHASHSEED` en la unidad systemd del VPS (no está en el `.env`) |
| G6 | 👤 dueño | faltan `NEON_DATABASE_URL`, `NEON_DATABASE_URL_POOLED`, `COHERE_API_KEY`, `OPENAI_API_KEY` en Actions |
| Decisiones 14-sep | ✅ delegadas y elegidas · lote 44 las aplica | `decisiones_dueno_2026_09_14.md`: juez sí ($1, tras el lote 38) · E5-B cohorte = canario del dueño, topes heredados · claras en botella ≥ 4 · V7a residuo · B7 pesos 2.0/1.0 solo canario · equipo se queda · D8 cerrar · G1 `block` · F9 Juan Carlos Brito (fecha por confirmar). Con la anotación reponderada por estrato, 73 % de las comidas del corpus del 09-06 tenían defecto según el dueño |

## Cómo leerlo

- **P0** — afecta restricciones clínicas, identidad de ingredientes, cantidades o validez de lo entregado, HOY, a usuarios reales.
- **P1** — defecto entregable acotado o gate/knob que bloquea avanzar el roadmap.
- **P2** — deuda que hace caro el siguiente cambio; medición pendiente.
- **P3** — mejoras y ampliaciones.

## Fase A · Cerrar lo que esta verificación dejó abierto (1-2 semanas)

| # | Ítem | Prio | Fuente | Notas |
|---|---|---|---|---|
| A1 | Revisar, mergear y desplegar en canario la rama `claude/auditoria-arquitectura-2026-09-11` (12 correcciones, 69 tests) | P0 | esta verificación §6 | Cambia QUÉ come el canario (3/5/6 comidas, familia, mercado). Medir `_candidate_source`, `_sodium_unknown_lines` y días caídos al LLM en `pipeline_metrics` la primera semana. |
| A2 | Auditar las 14 sondas cuando llegue `BioBoros_auditoria_arquitectura.md` | P1 | esta verificación §2 | Criterio ya escrito; sin el archivo no hay nada que auditar. |
| A3 | `_line_sodium_mg` (camino LLM) coerce «sin dato» a 0 sin marca | P1 | H6a; `graph_orchestrator.py:30275` | Exige líneas en el god-file ⇒ hacer tras A6. |
| A4 | `review_plan_node` consume `_shopping_coherence_unevaluable`; fila «no evaluable» en `docs/coherence_surfaces_table.md` (referencias de línea obsoletas) | P1 | H9 | El flag ya se persiste; falta que el veredicto y el dashboard lo vean. |
| A5 | Re-encolar la proyección al cambiar precios (`routers/supermarket.py`, migraciones de `master_ingredients`) y `householdSize` | P1 | H12 | Hoy sólo la Nevera entra en la huella. |
| A6 | Refactor mínimo del god-file: extraer `_SLOT_KEY_MAP`+`_canonical_slot_fractions` a `nutrition_calculator` y `_line_sodium_mg`/`_meal_sodium_mg` a `nutrition_db` | P1 | `graph_orchestrator.py` = 53.100 = tope | Libera líneas para A3/A4 y quita los imports perezosos desde `deterministic_day`. Solapa con ARQ30-P2-01 / Fase 9. |
| A7 | `apply_library_recipe` (camino LLM): el agua de la receta congelada no escala porque no hay factor | P2 | H4 | Derivar factor implícito (constituyente ancla) o marcar `_recipe_water_unscaled`. |
| A8 | Docs obsoletos: `plan_policy_f3.md:91` (dice abierto lo que ARQ27-P1-07 cerró), `dish_registry_f6.md:48` (`prep_minutes_est` «por técnica») | P2 | esta verificación §3 | Un doc que reabre un gap cerrado cuesta una sesión. |
| A9 | Re-firmar la revisión curatorial de los 6 perfiles tras los 88 platos veganos | dueño | `ESTADO-IMPLEMENTACION.md`; `cultural_profiles_f7.md:94-96` | `_signoff_for` devuelve `None` para todos; es fail-safe pero la firma no existe. |

## Fase B · Determinismo y personalización medibles (roadmap 2.5 F3/F4 + auditoría)

| # | Ítem | Prio | Fuente |
|---|---|---|---|
| B1 | Métrica de personalización completa: códigos `anchor_portion_*`, `prep_time_over_budget`, `culture_share_*`, `equipment_unavailable`; el score no puede ser 1,0 con `n_checks = 2` | P1 | H11; `horizon.py:1467-1472` |
| B2 | Benchmark clínico dirigido con `enforce` vivo (cuesta LLM) ⇒ promover `MEALFIT_FIDELITY_GATE` `warn→block` | P1 / dueño | roadmap Fase 3; `plan_policy_f3.md:87` |
| B3 | Huella de la computación (`computation_hash`: snapshot real, catálogo extendido, prompt, modelo, temperatura, semilla) separada de `input_hash` | P1 | H10, H13 |
| B4 | Semilla determinista en seeder y `random_seed` del prompt; `PYTHONHASHSEED` en `Procfile` | P2 | H13; `ai_helpers.py:1144…`, `graph_orchestrator.py:7584` |
| B5 | «Ninguna cocina» (perfil `neutral`), `diet.exclusions` al registry, código de relajación `culture_unavailable`; cultura por FRANJA (el blueprint ya la guarda) | P1 | H15; `cultural_profiles.py:210-211`, `horizon.py:511-515` |
| B6 | Memoria entre días en `deterministic_day` (tope semanal `balanced` roto 9 ventanas incluso con las puertas apagadas) ⇒ después decidir `MEALFIT_DETERMINISTIC_DAY_SAME_DAY_VARIETY` | P1 | `deterministic_day.py:475-489`; memoria 09-10 |
| B7 | Sesgo de macros del día determinista (proteína −18 %, carbos altos): palancas reales = scorer de `elegir_plantilla` + composición de la biblioteca (plantillas bajas en carbohidrato) | P1 / dueño | `docs/deterministic_day.md:96` |
| B8 | Flip global del día determinista (`MEALFIT_RECIPE_LIBRARY_SELECT` ya en 1; `MEALFIT_DETERMINISTIC_DAY` global) tras A1 y B6 | dueño | `docs/deterministic_day.md:12-18` |
| B9 | Embudo del wizard (Fase 4): línea base de ≥2 semanas (`pipeline_metrics.wizard_funnel`) | P2 | `plan_policy_f4.md` |

## Fase C · Coherencia culinaria (backlog CUL, 17 tareas, ninguna implementada)

| # | Ítem | Prio | Fuente |
|---|---|---|---|
| C0 | Prerrequisitos: línea base con huella de corpus FIJO (la del 6-sep dejó de ser reproducible en 14 h) y las **80 etiquetas humanas** | P0 (bloquea todo C) / dueño | `2026-09-07-coherencia-culinaria/REVISION-DE-LOS-GAPS.md` |
| C1 | CUL-P0-01 identidad/versión/estado de evaluación de cada hallazgo; CUL-P0-02 referencia independiente y scorer estricto | P0 | `BACKLOG-P0-P3.md:9,:21` |
| C2 | CUL-P0-03 contrato sobre la receta final y sus reparaciones (sustituciones que dejan técnicas del alimento anterior; frecuencia sin medir) | P0 | `BACKLOG-P0-P3.md:33` |
| C3 | CUL-P0-04 huevo entero/clara/yema no intercambiables (tres reglas que se refuerzan; `MAX_EGG_WHITES_PER_MEAL` no basta) | P0 | `BACKLOG-P0-P3.md:45` |
| C4 | ~~Asignación paso↔ingrediente en la receta congelada (`pasos[i].usa`), V6 de «puede repartir» a suma exacta~~ → hecho, ver Estado | P1 | H8; `culinary_coherence.py:1158-1161` |
| C5 | ~~CUL-P1-01…07 (porciones de clara, estructura del plato, cultura y franja como contexto, ajustar nutrición sin desmontar la receta, cantidades/estados/tiempos/equipo ejecutables, juez que admite creatividad, benchmark en todas las superficies)~~ → en curso, ver Estado | P1 | `BACKLOG-P0-P3.md:59-123` |
| C6 | `MEALFIT_CULINARY_JUDGE_GUARD` nace `off` en prod; promover a `warn` cuando C1 haga interpretable el veredicto · **medido (`P1-PLAN-LOTE-63` · 2026-09-15)**: producción ya corre en `warn` (`prod_profile.py`, VPS 09-06), así que lo que queda es `warn` → `block`; el juez da 0 % de precisión estricta y 82,1 % por sustancia (el mismo defecto que el dueño, con otro código); `paso_incoherente`, `tecnica_impropia` y `nombre_no_corresponde` en observación (`MEALFIT_CULINARY_JUDGE_OBSERVACION_CODES`). Recomendación: no pasar a `block` hasta alinear códigos↔clases con un 2.º anotador (`culinary_coherence.md` §«El juez por código») | dueño | `graph_orchestrator.py:6443`; `prod_profile.py:42` |
| C7 | CUL-P2-01…04 y CUL-P3-01…03 (variedad perceptible 7/15/30, presupuesto de reparaciones, deriva, cocinado real; gustos de preparación, biblioteca de variantes, «magia» con usuarios) | P2-P3 | `BACKLOG-P0-P3.md:135-191` |
| C8 | 4 plantillas sin receta y 2 sustituciones con nombre heredado (Chillo⇢pescado blanco, Salami de pavo⇢jamón de pavo): un plato sustituido necesita su identidad | P1 / dueño | `docs/deterministic_day.md`; `ESTADO-IMPLEMENTACION.md` |

## Fase D · Lista de compras y Nevera

| # | Ítem | Prio | Fuente |
|---|---|---|---|
| D1 | «1 sobre» de sazón entra como 40 g (peso de la CAJA, no del sobre: `density_g_per_unit`=5) ⇒ necesidad 8× y aviso falso con 2+ sobres/semana; mueve la cantidad esperada del guard | P1 | memoria `project_aviso_capado_lee_el_envase_2026_09_11.md` #1 |
| D2 | «40 g de Cebolla en polvo» resuelve a Cebolla fresca (½ lb) | P1 | ídem #2 |
| D3 | `_apply_condiment_sanity_cap` reescribe `display_qty` pero no `display_string` | P2 | ídem #3 |
| D4 | Quién escribe el literal `30` conservando la unidad (cantidades absurdas): 3-4 comidas de 1.194; decidir barrido | P2 / dueño | `hallazgo_abierto_cantidades_absurdas_en_compra.md:112` |
| D5 | Gate de Nevera: umbral mínimo de cantidad (7 g de arroz bloquea como 200 g de pollo), unidad `malla`, 4 lecturas sueltas del flag en `_chunk_worker` | P2 | `pantry_gate_fourth_guard.md:143` |
| D6 | `MEALFIT_TRIP_WINDOWED_PERISHABLES` OFF con 4 prerrequisitos y un bug latente en esa rama (`shopping_calculator.py:13892`) | P2 | `knobs_reference.md` |
| D7 | `MEALFIT_GUARD_UNDERSUPPLY_SEVERE`: medir volumen real de `magnitude_undersupply` antes de encender (encendido ⇒ dead letter por reintento idéntico) | P2 | `shopping_calculator.py:7138` |
| D8 | Fase 5 `shopping_commercial` (marcas/presentación por producto) y medición de lag p95 de `plan_jobs` | P3 | `plan_jobs_f5.md:100` |
| D9 | Supermercado: imágenes, cadencia de precios, más familias; Fase 2 de derivación de masters (dueño) | P3 / dueño | `supermarket_db.md:99` |

## Fase E · Roadmap 2.5 sin cerrar y auditoría 2.7/3.0

| # | Ítem | Prio | Fuente |
|---|---|---|---|
| E1 | Fase 0 «higiene de entrega»: tag `baseline-v1`, retirar worktrees viejos, deploy desde tag con `git status` limpio, inventario de knobs/crons/alert_keys. **Hoy el deploy empaqueta el árbol de trabajo y hay 2 repos con cambios sin commitear** (root: 8 modificados + 2 docs; frontend: ~24 modificados) | P0 (operativo) / dueño D11 | roadmap §12 Fase 0; `git status` |
| E2 | Fase 1 gate: 2 recuperaciones simuladas con kill + 7 días sin alertas ⇒ flip `MEALFIT_INITIAL_VIA_QUEUE` | P1 | roadmap Fase 1 «Estado 2026-09-02» |
| E3 | ARQ27-P2-01 trazabilidad de checkout/evidencia (`open_confirmed`) | P2 | `GAPS.md:593` |
| E4 | ARQ27-P1-06 canario de latencia/coste + swap + último chunk punta a punta | P1 | `ESTADO-IMPLEMENTACION.md` |
| E5 | ARQ30-P1-01 promover la representación canónica a compras reales (canario por cohorte) | P1 | `arq30_f4_canonical.md` |
| E6 | ARQ30-P1-03 asignación del horizonte por comidas viables; P1-04 una autoridad para porciones y reparaciones; P1-05 commit de la revisión validada en todas las superficies | P1 | `GAPS.md:284,:307,:330` |
| E7 | ARQ30-P2-01…05 (god files, aprendizaje de preferencias con evidencia útil, disponibilidad/coste por mercado con incertidumbre — sin export privado —, latencia/coste por resultado útil, adecuación nutricional separada de cobertura) | P2 | `GAPS.md:353-441` |
| E8 | Fases 8 (medios), 9 (refactor y migraciones de contrato), 10 (benchmark final y publicación) | P3 / dueño D8, D10 | roadmap §12 |
| E9 | Cruce flojo: vegetariano · día 25 sin congelador en ES deja 1 almuerzo | P2 | `ESTADO-IMPLEMENTACION.md` |

## Fase F · Países, i18n, calidad de datos

| # | Ítem | Prio | Fuente |
|---|---|---|---|
| F1 | Aplicar en Neon `p3_country_db_check_2026_08_22.sql` (única migración sin fila en el libro) | P1 / dueño | `migrations_ledger.md:46`; G41 |
| F2 | Reconciliar el estado de G01-G96 (sólo G13/G27/G29 anotados como cerrados; la memoria registra olas que no se escribieron) | P2 | `2026-08-23-paises-gaps-tareas.md` |
| F3 | i18n v3 condición de salida: corrida real por idioma con `_display[locale]` verificado por SQL (0 de 50 planes hoy), recorrido humano en la app nativa, 3 CIs verdes | P1 | `2026-08-23-i18n-produccion-gaps-v3.md` |
| F4 | `SIBLING_REPO_TOKEN` ausente en el repo backend ⇒ su CI corre 0 tests (dos rojos latentes sólo vistos a mano) | P0 (CI) / dueño | ídem «Para el dueño» |
| F5 | Auditoría de «guards inertes» nunca ejecutada; hacerla en worktree aislado (el primer intento dejó 8 mutaciones en producción) | P1 | ídem |
| F6 | 9 filas del catálogo en proxy (chiles MX, xoconostle, embutidos latinos, guineo verde, requesón); barrido descripción-USDA vs nombre para cazar el `fdc_id` mal apuntado | P2 | `catalog_provenance_audit.md:134` |
| F7 | 5 filas sin `phosphorus_mg` que sostienen 7 constituyentes (renal los exige) | P1 | `ESTADO-IMPLEMENTACION.md` |
| F8 | 6 knobs divergen entre producción y la suite (`MEALFIT_COUNTRY_SYSTEM` prod `true`/suite `False` ⇒ ningún test vio los 5 catálogos beta) | P1 | ídem ARQ27-P1-06 |
| F9 | Validación clínica independiente: ingesta de CSV revisado por nutricionista + gate por % de aprobación (bloqueado en la primera revisión humana) | P2 / dueño | `clinical_independent_validation.md:63` |
| F10 | Marker `P1-COUNTRY-CAPS-DO-LEXICON` sin línea en `MEMORY.md` (`test_p3_marker_memory_xlink` lo acusa en cada deploy) | P2 | i18n v3 «Para el dueño» |

## Fase G · Billing, frontend, infra

| # | Ítem | Prio | Fuente |
|---|---|---|---|
| G1 | Override de importe en PayPal: en `block` sólo se marca `proven_underpaid` con cupón; sin cupón nunca se bloquea y `discount_codes` está vacío ⇒ cualquier override es ilegítimo por definición. Decidir `MEALFIT_BILLING_VERIFY_AMOUNT` + bloquear sin cupón activo → ✅ 2026-09-15 `P1-PLAN-LOTE-61`: default `block` y bloqueo sin ningún cupón activo para el tier | P0 / dueño | `paypal_audit_2026_08_22.md:128`; `routers/billing.py:635` |
| G2 | `plan_tier` `'free'` en esquema vs `'gratis'` en código; 6 usuarios `e2e-test-*` en producción; un `plus` sin `paypal_subscription_id` | P1 | `paypal_audit_2026_08_22.md` §2-3 |
| G3 | Borrado de cuenta no purga la identidad en Neon Auth (único `TODO` real del backend; incidente de cuenta resucitada 08-sep) | P1 | `routers/system.py:1063`; memoria 09-08 |
| G4 | Ramas sin mergear: `worktree-perf-frontend-fluidez` (entry 187→147 kB; la integra el dueño), `ci/backend-layout-fix`, `paises-ola-2` (×2), `pr8`, `wip/paused-banner-no-flash` | P1 / dueño | `git branch --no-merged main` |
| G5 | `P2-CSP-ENFORCE` (Report-Only → enforce, radio total); `P2-THEME-TOKENS-BOYSCOUT` (60 overrides) | P2 / dueño | `2026-08-14-landing-produccion-design.md` |
| G6 | Benchmark nocturno de macros auto-saltado: 4 secretos de GitHub; después refrescar `macro_baseline.json` y estrechar tolerancias | P1 / dueño (5 min) | `nightly_benchmark_activation.md` |
| G7 | iOS Fase 2 (APNs, deep links, Codemagic, ficha) tras la membresía de Apple; `P2-I18N-IOS-STORE-SOLO-ES` ya es P1 (la app está en App Store Connect) | P1 / dueño | `2026-08-21-ios-native-shell-design.md`; i18n v3 |
| G8 | Landing/comercial/legal: analítica del landing (consentimiento), oferta del 15-sep, «10 créditos» vs 15 prometidos, claims de «comida dominicana» a seis mercados, GDPR con España en venta, unidades por defecto fr/it/pt | dueño | specs landing y planes de países |

## Decisiones que sólo puede tomar el dueño (resumen)

D1-D11 del roadmap (§16), más: re-firma curatorial (A9); benchmark clínico con `enforce` (B2); 80 etiquetas humanas (C0); 4 secretos del benchmark nocturno (G6); `SIBLING_REPO_TOKEN` (F4); migración G41 (F1); override de PayPal (G1); integrar `worktree-perf-frontend-fluidez` (G4); flips `MEALFIT_INITIAL_VIA_QUEUE`, `MEALFIT_FIDELITY_GATE`, `MEALFIT_DETERMINISTIC_DAY_SAME_DAY_VARIETY`, `MEALFIT_TRIP_WINDOWED_PERISHABLES`, `MEALFIT_GUARD_UNDERSUPPLY_SEVERE`, `MEALFIT_CULINARY_JUDGE_GUARD` (cada uno con su condición previa escrita).

## Orden recomendado (si sólo hay una persona)

1. **Semana 1**: A1 (canario y medición), F4 (CI que no corre tests), G1 (override de PayPal), E1 (árboles sucios: el deploy empaqueta lo que hay), F1.
2. **Semana 2**: A6 → A3/A4, A5, C0 (línea base fija + pedir las 80 etiquetas), B6.
3. **Semanas 3-4**: B1 + B2 → gate de fidelidad a `block`; B5; C1-C3; D1-D2.
4. **Después**: B3/B4 (huella de la computación y semilla), C4/C5, E2-E6, F3/F5, y las fases 8-10 con las decisiones del dueño.
