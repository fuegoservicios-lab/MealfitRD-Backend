# Tabla canónica `system_alerts` resolution

> Tabla movida de CLAUDE.md (P2-AUDIT-4 · 2026-05-10) por presión de tamaño en CLAUDE.md (carga en cada turn). El test parser-based [`test_p2_audit_4_alert_keys_documented.py`](../backend/tests/test_p2_audit_4_alert_keys_documented.py) parsea este archivo directamente para drift detection bidireccional con los call sites en `cron_tasks.py`/`db_inventory.py`/`memory_manager.py`/`app.py`/`graph_orchestrator.py`/`routers/billing.py`.

## Política de `system_alerts` resolution

[P2-NEW-3 · 2026-05-10 · reconciliada P2-AUDIT-4 · 2026-05-10] Modelo: **upsert por `alert_key` + `resolved_at` mutable** (alert "vive" mientras `resolved_at IS NULL`). 4 modelos de resolution: **Auto (explicit)** productor emite UPDATE resolved_at; **Auto (implicit)** productor solo re-emite mientras condición existe, operador depende del staleness; **Handler-driven** endpoint del usuario cierra; **Manual** SRE (SOPs detallados en [`runbook_system_alerts_sops_2026_05_11.md`](../../.claude/projects/C--Users-angel-OneDrive-Escritorio-MealfitRD-IA/memory/runbook_system_alerts_sops_2026_05_11.md)).

| `alert_key` (pattern) | Productor | Resolver | Modelo |
|---|---|---|---|
| `scheduler_missed_<job_id>` | `_scheduler_alert_listener` (app.py) `EVENT_JOB_MISSED` | listener `EVENT_JOB_EXECUTED` (P1-NEW-2) + sweep `_resolve_stale_scheduler_alerts` TTL 24h (P0-AUDIT-1) + startup-run en `lifespan` (P0-NEW-1-AUTOHEAL) | Auto (explicit) |
| `scheduler_error_<job_id>` | mismo listener, `EVENT_JOB_ERROR` | mismo path que `scheduler_missed_*` | Auto (explicit) |
| `scheduler_cascade_missed` | (a) cron `_alert_scheduler_cascade_missed` (alert_type=`scheduler_cascade`) cada 30min — SSOT primario; (b) **listener inline P1-CASCADE-INLINE · 2026-05-27** (`_scheduler_alert_listener.app.py` → `_maybe_emit_inline_cascade_alert`) cuando ≥N distinct jobs MISSED en ventana 60s — detección sub-minuto, mismo `alert_key`/UPSERT, knob `MEALFIT_SCHEDULER_CASCADE_INLINE_ENABLED` kill-switch + autohealer P0-NEW-2 (Sentry + `_resolve_stale_scheduler_alerts`) | estabilización P2-LIVE-1 (0 children, `MEALFIT_SCHEDULER_CASCADE_STABILIZATION_MIN` default 60min) OR hard-cap P2-NEW-E edad absoluta `MEALFIT_SCHEDULER_CASCADE_HARD_CAP_HOURS` default 2h (P2-AUDIT-3) | Auto (explicit) |
| `coherence_watchdog_silent` | cron `_alert_coherence_watchdog_silent` (P0-3 liveness) | cron re-eval | Auto (implicit) |
| `deploy_lag_marker_malformed` | cron `_alert_deploy_lag_marker_stale` cuando `_LAST_KNOWN_PFIX` no matchea regex `Pn-X · YYYY-MM-DD` | cron re-eval tras fix | Auto (implicit) |
| `deploy_lag_marker_stale` | mismo cron cuando `_LAST_KNOWN_PFIX` < floor de fecha | cron re-eval tras bump | Auto (implicit) |
| `deploy_lag_drift_vs_expected` | mismo cron cuando live ≠ `expected_last_known_pfix` (alert_type=`deploy_drift`); SOP en runbook | cron re-eval tras deploy o update KV | Auto (implicit) |
| `clinical_band_drift` | cron `_clinical_band_drift_alert_job` (P4-SCOREBOARD) cuando el `clinical_band_score` promedio de la flota (métrica `clinical_band`, no-fallback) cae < umbral (default 0.45) sobre ≥N planes (alert_type=`precision_drift`) | cron re-eval cuando vuelve sobre el umbral (UPDATE `resolved_at`) | Auto (implicit) |
| `plan_fallback_rate_high` | cron `_plan_fallback_rate_alert_job` (P1-MACRO-ROLLOUT-OBS) cuando la fracción de planes entregados como fallback matemático (métrica `clinical_band`, `delivered_was_fallback='true'`) supera el umbral (default 0.25) sobre ≥N planes (alert_type=`reliability_degradation`). Prerequisito de observabilidad para el rollout del motor de macros (`MEALFIT_MACRO_SOLVER_ENABLED`): vigila que activar el solver no dispare el fallback | cron re-eval cuando vuelve bajo el umbral (UPDATE `resolved_at`) | Auto (implicit) |
| `failed_inventory_deductions_backlog` | cron `_alert_failed_inventory_deductions_backlog` > knob default 25 (24h lookback) | manual tras drenar backlog | Manual |
| `plan_data_corrupted:<plan_id>:<field_name>` | mutators que detectan plan_data inválido (cron_tasks.py); SOP en runbook | manual (rollback vs hotfix) | Manual |
| `reservation_reconciliation_exhausted:<plan_id>:<week>` | reconciliador de reservas tras N retries fallidos | manual: recuperar chunk + liberar reservas | Manual |
| `dead_lettered_chunk:<plan_id>:<week>` | `_escalate_unrecoverable_chunk` (P2-A sealing) | `regenerate-simplified` re-encola | Handler-driven |
| `chronic_deferrals:<user_id>` | cron `_detect_chronic_deferrals` cuando deferrals TZ ≥ umbral | [G11-CHRONIC-RESOLVE · 2026-05-30] mismo cron vía `_resolve_cleared_chronic_deferral_alerts`: UPDATE resolved_at cuando el usuario ya NO está en el set activo (TZ corregida / plan completa). Pre-fix NUNCA resolvía → resolved_at IS NULL para siempre | Auto (explicit) |
| `chunk_lesson_synth_ratio_high` | cron `_alert_high_synthesized_lesson_ratio` synth:real > umbral 24h | [G6-SYNTH-RATIO-RESOLVE · 2026-05-29] mismo cron: UPDATE resolved_at cuando `total >= MIN_SAMPLES` y `ratio < umbral` (recuperado). Pre-fix NUNCA resolvía (bare-return) | Auto (explicit) |
| `chunk_synthesis_overload:<user_id>:<plan_id>:<week>` | helper P0-B `_pause_chunk_for_synthesis_overload` (alert_type=`chunk_synthesis_overload_per_user`) | cron `_alert_high_synthesized_lesson_ratio` resolve cuando ya NO hay chunk pausado con `synthesis_ratio_exceeded` que matchee la key (E1-SYNTH-OVERLOAD-RESOLVE · 2026-05-29) | Auto (explicit) |
| `atomic_pool_fallback_active` | cron `_alert_atomic_pool_fallback` (P2-6) | cron sweep marca resolved si `last_at` excede window | Auto (explicit) |
| `dead_lettered_chunks_recent` | cron `_alert_new_dead_lettered_chunks` (agregado flota) | `regenerate-simplified` re-encola último **+ [P2-CHUNK-9] auto-resolve** cuando `total < MIN_COUNT` (backlog despejado por regen/GC/ventana) | Handler-driven + Auto (explicit) |
| `chunks_tz_unresolved_stuck` | cron `_alert_chunks_stuck_in_tz_unresolved` | cron re-eval **+ [P2-CHUNK-9] auto-resolve** cuando no quedan chunks tz-stuck | Auto (explicit) |
| `chunks_stuck_processing` | [P2-CHUNK-9] cron `_alert_chunks_stuck_processing` (chunks en `status='processing'` > `MEALFIT_STUCK_PROCESSING_HOURS`, default 2h — zombie de WORKER que el reclaim heartbeat-aware no recuperó) | mismo cron: UPDATE resolved_at cuando no quedan chunks stuck | Auto (explicit) |
| `lesson_telemetry_flush_backlog` | [P2-CHUNK-9] `_track_lesson_telemetry_backlog` vía `_track_cron_consecutive_failure` cuando el buffer de `chunk_lesson_telemetry` supera el umbral ≥3 ticks consecutivos (DB degradada sostenida / flush stuck) | tracker SSOT auto-resuelve en el primer tick sano (buffer drenado bajo umbral) | Auto (explicit) |
| `deferrals_flush_backlog` | [P3-PROD-AUDIT-3 · 2026-05-30] `_track_deferrals_backlog` vía `_track_cron_consecutive_failure` cuando el buffer de `chunk_deferrals` supera `MEALFIT_DEFERRALS_BUFFER_BACKLOG_THRESHOLD` (default 50) ≥3 ticks consecutivos (DB degradada sostenida / flush stuck → ciega la detección de `chronic_deferrals`). Hermano simétrico de `lesson_telemetry_flush_backlog` que faltaba | tracker SSOT auto-resuelve en el primer tick sano (buffer drenado bajo umbral) | Auto (explicit) |
| `chunk_pantry_snapshots_stale` | cron `_alert_chunk_pantry_snapshots_stale` (chunks pending con `_pantry_captured_at IS NOT NULL`, P1-LIVE-1) | P0-AUDIT-2: early-return UPDATE resolved_at cuando `len(rows) < min_count` | Auto (explicit) |
| `chunk_lag_excessive` | cron `_alert_chunk_lag_excessive` (agregado flota) | [G7-LAG-RESOLVE · 2026-05-29] mismo cron: early-return UPDATE resolved_at cuando `len(rows) < min_count` (lag despejado). Pre-fix NUNCA resolvía (bare-return) | Auto (explicit) |
| `chunk_dual_processing_detected` | cron `_alert_chunk_dual_processing` (severity critical) | Manual: investigar race entre instances | Manual |
| `chunk_paused_indefinitely:<plan_id>:<week>` | cron `_alert_chunks_paused_indefinitely` | `regenerate-simplified` + cron re-eval | Handler-driven |
| `degraded_rate_high:<tipo>` | `_persist_quality_degradation_alert`; tipo ∈ `{refill, initial}` | cron `_alert_if_degraded_rate_high` resuelve (UPDATE resolved_at) cuando ratio ≤ 0.15 con muestra ≥10 (E2-DEGRADED-RESOLVE · 2026-05-29) | Auto (explicit) |
| `temporal_gate_proactive:<user_id>:<plan_id>:<week>` | hook P1-3 (severity info, no failure) | [G12-TEMPORAL-GATE-RESOLVE · 2026-05-30] `_check_chunk_learning_ready`: UPDATE resolved_at cuando el gate finalmente pasa (`_p12_ready`). Pre-fix NUNCA resolvía → resolved_at IS NULL para siempre | Auto (explicit) |
| `inventory_rpc_fallback` | `apply_inventory_delta` except path (P1-NEW-1) | manual tras verificar RPC operativa | Manual |
| `chunk_learning_stub_overload:<plan_id>` | `_chunk_worker` cuando `_chunk_learning_stub_count ≥ 2` (P1-AUDIT-1) | manual: investigar logs | Manual |
| `shopping_list_replace_partial_rollback:<user_id>` | `replace_user_inventory_with_shopping_list` DELETE-then-INSERT falló parcialmente (P3-1) | Manual: logs `[P3-D/ROLLBACK]` + re-INSERT manual | Manual |
| `memory_summary_failures` | `_persist_summary_failure_alert` cuando `summarize_and_prune` falla N veces (P1-19) | manual tras fix modelo/API-key/cuota | Manual |
| `dreaming_consolidation_failures_burst` | [P1-DREAMING-1 · 2026-06-13] `_dream_consolidate_facts` vía `_track_cron_consecutive_failure` cuando el cron de Dreaming falla ≥2 corridas (import roto / `run_dream_cycle` excepción) | tracker SSOT auto-resuelve en el primer tick sano | Auto (explicit) |
| `dreaming_budget_exhausted` | [P1-DREAMING-1] `_dream_consolidate_facts` vía `_emit_dreaming_alert` cuando el ciclo nocturno alcanza `MEALFIT_DREAMING_MAX_COST_USD_PER_NIGHT` (resto de usuarios diferido a la próxima noche). severity=warning | Auto (implicit): re-emite mientras dure; la key de budget lleva fecha → reset natural diario | Auto (implicit) |
| `dreaming_backlog_high` | [P1-DREAMING-1] `_dream_consolidate_facts` vía `_emit_dreaming_alert` cuando `dream_work_queue` pendiente > `MEALFIT_DREAMING_BACKLOG_ALERT` (default 1000): el cron no drena al ritmo de encolado | mismo cron: UPDATE resolved_at cuando backlog ≤ umbral | Auto (explicit) |
| `macro_engine_disabled_in_prod` | [P2-CRITICAL-CONFIG-ALERT · 2026-06-15, gap-audit G7] `get_critical_config_warnings` (graph_orchestrator.py) → emitido por `_emit_critical_config_alerts` (app.py lifespan startup) cuando `MEALFIT_MACRO_SOLVER_ENABLED=False` en ENVIRONMENT=production (motor de precisión "a ojo", proteína ~16% MAPE) | startup check auto-resuelve (UPDATE resolved_at del `critical_config` ausente del set activo) cuando el motor vuelve ON + redeploy | Auto (explicit) |
| `safety_guard_disabled_in_prod:<knob>` | [P2-CRITICAL-CONFIG-ALERT · 2026-06-15, gap-audit G10] mismo `get_critical_config_warnings`/`_emit_critical_config_alerts` cuando un guard de seguridad clínica (`MEALFIT_RENAL_CAP`/`ALLERGEN_HARD_GUARD`/`ALLERGEN_SUBSTITUTION`/`PRO_REVIEW_FLAG`/`CONDITION_RULES`/`PROTEIN_FLOOR_HARD_GATE`) está OFF en production | mismo startup check auto-resuelve cuando el guard vuelve ON + redeploy | Auto (explicit) |
| `macro_reconcile_degraded_in_prod` | [P2-RECONCILE-DEGRADED-ALERT · 2026-06-16, gap-audit P2-7] mismo `get_critical_config_warnings`/`_emit_critical_config_alerts` cuando el solver está ON pero `MEALFIT_MACRO_AWARE_RECONCILE` y/o `MEALFIT_MACRO_POSTQUANT_RECONCILE` están OFF en production (el split C:F no se clava → carbos/grasas ~19-22% MAPE) | startup check auto-resuelve cuando el reconcile vuelve ON + redeploy | Auto (explicit) |
| `clinical_layer_incomplete` | [P2-CLINICAL-LAYER-CONSUMER · 2026-06-15, gap-audit G8] `_maybe_mark_clinical_layer_incomplete_degraded` (graph_orchestrator.py, path de scoring) cuando la capa clínica determinista corrió incompleta (DB de nutrición no cargó → sin enforcement renal per-comida; o proteína renal compuesta no-resuelta) para un perfil con condición/alergia real | Manual: investigar import roto de `IngredientNutritionDB` o cobertura del catálogo; resolver tras fix | Manual |
| `dream_contradiction:<user_id>` | [P1-DREAMING-1] `_persist_dream_contradiction_alert` vía `_emit_dreaming_alert` cuando el Dreaming detecta facts en conflicto (NUNCA auto-muta facts clínicos) | Manual: revisar si involucra alergia/condición médica; corregir el fact vía Ajustes o endpoint | Manual |
| `plan_quality_degraded:<user_id>:<plan_id>` | `_emit_plan_quality_degraded_alert` (graph_orchestrator, P1-NEW-3) en 5 ramas "end" de `should_retry`; `metadata.caller_context` (P1-NEW-9) distingue `initial_generate` vs `jit_week2` | cron `_resolve_stale_plan_quality_alerts` (P2-NEW-10, default 60min) cierra si existe meal_plan posterior `complete` AND sin `_is_fallback`/`_review_failed_but_delivered`. Knob `MEALFIT_PLAN_QUALITY_AUTO_RESOLVE_ENABLED` | Auto (explicit, plan-posterior-limpio) |
| `post_swap_critical_divergence_<key_id>` | `_emit_post_swap_coherence_alert` (graph_orchestrator) swap deja N≥knob (default 3) divergencias críticas; cooldown per-user 6h, severity=warning | mismo cron P2-NEW-10 por edad `MEALFIT_POST_SWAP_DIVERGENCE_AUTO_RESOLVE_HOURS` default 24h (hard-floor 6h) | Auto (explicit, edad post-cooldown) |
| `hot_table_bloat:<table>` | cron `_emit_hot_table_bloat_tick` (P2-AUDIT-2, cada 6h default, knob `MEALFIT_AUTOVACUUM_TICK_INTERVAL_MIN`) cuando `dead_pct>70 AND hours_since_av>24` sobre 7 tablas P1-B; **P1-HOT-BLOAT-MIN-TUPLES** skip si total_tuples < `MEALFIT_AUTOVACUUM_MIN_TOTAL_TUPLES` (default 100) → emite `_hot_table_bloat_skipped_low_volume` + auto-resuelve. severity=warning | mismo cron: tabla sana (dead_pct ≤ umbral O hours_since_av ≤ umbral) O total_tuples bajo umbral → UPDATE resolved_at | Auto (explicit, mismo cron) |
| `pipeline_metrics_silence` (1 nodo) / `pipeline_metrics_silence:<node>` (multi-nodo) | cron `_alert_pipeline_metrics_silence` (P2-OBSERVABILITY-1 + P2-PIPELINE-SILENCE-MULTI-NODE) cada `MEALFIT_PIPELINE_METRICS_SILENCE_INTERVAL_MIN` default 10min; `MEALFIT_PIPELINE_METRICS_SILENCE_NODE` acepta CSV `<node>:<threshold_min>` per-entry; captura deploy-lag/hardfloor cancelado/drift schema. severity=warning | mismo cron: tick vuelve → UPDATE resolved_at per-`alert_key` (per-nodo en multi-nodo) | Auto (explicit, mismo cron) |
| `billing_old_sub_cancel_failed:<user_id>:<old_sub_id>` | `_persist_billing_alert` en `/api/subscription/verify` upgrade-path (P1-BILLING-UPGRADE-FAIL-LOUD, routers/billing.py) cuando PayPal `/cancel` non-idempotent-failure; handler aborta HTTP 409 ANTES del UPDATE plan_tier (previene doble cobro). severity=critical | Manual: verificar PayPal dashboard, cancelar manual sub vieja si ACTIVE, UPDATE resolved_at | Manual |
| `billing_cancel_failed:<user_id>:<sub_id>` | `_persist_billing_alert` en `/api/subscription/cancel` (P1-BILLING-CANCEL-FAIL-LOUD) cuando OAuth o `/cancel` non-idempotent; handler aborta HTTP 502 ANTES del UPDATE subscription_status; `metadata.stage` ∈ {oauth, cancel}. severity=critical | Manual: verificar PayPal, cancelar manual, marcar BD CANCELLED + cerrar alert | Manual |
| `billing_profile_not_found_on_upgrade:<user_id>:<subscription_id>` | `_persist_billing_alert` en `/api/subscription/verify` (P1-PROD-AUDIT-3, routers/billing.py) cuando PayPal YA validó/cobró pero el UPDATE de `user_profiles` matcheó 0 filas (perfil ausente — patológico, `handle_new_user` lo crea al signup); handler aborta HTTP 500. El usuario pagó pero el tier no se aplicó. severity=critical | Manual: localizar el perfil/sub en PayPal + crear/reparar `user_profiles` con el tier correcto, UPDATE resolved_at | Manual |
| `pdf_stale_inventory_fallback_burst` | cron `_alert_pdf_stale_inventory_fallback_burst` (P2-SHOPPING-3) cuando count `pdf_stale_inventory_fallback` ≥ `MEALFIT_PDF_STALE_FALLBACK_ALERT_THRESHOLD` default 10/60min; eventos via `/api/plans/telemetry/pdf-stale-fallback` (fire-and-forget desde `Dashboard.jsx::handleDownloadShoppingList` cuando `fetchFreshInventoryWithTimeout` stale=true). severity=warning | mismo cron: count cae bajo umbral → UPDATE resolved_at (evento transient: network blip / pool exhausted) | Auto (explicit, mismo cron) |
| `chat_checkpoint_pool_split_missing` | `_emit_checkpoint_pool_split_missing_alert_best_effort` (agent.py, P3-CHAT-OBSERVABILITY · 2026-05-20) cuando `chat_with_agent`/`chat_with_agent_stream` cae al fallback `connection_pool` (Transaction Pooler) porque `chat_checkpoint_pool` (session-mode 5432) no se creó al arranque — reabre el modo SSL bad length/EOF que P1-CHECKPOINT-POOL-SPLIT cerró. Cooldown 1h in-process previene contención. severity=warning | Manual: revisar logs de `db.py` startup por errores en la creación del split pool; UPDATE resolved_at tras reparar | Manual |
| `gemini_spend_cap_exceeded` | `_persist_gemini_spend_cap_alert` (graph_orchestrator.py, P1-SPEND-CAP-ALERT · 2026-05-28) cuando el catch `EXTREME GRACEFUL DEGRADATION` detecta vía `_is_gemini_spending_cap_error` el 429 "monthly spending cap" de Gemini. alert_key GLOBAL (project-wide, no per-user) → dedupe por upsert. severity=critical. El pipeline también marca `plan_result._llm_spend_cap` para que routers/plans.py dé mensaje honesto al usuario (reintentar no ayuda) | Manual: subir/quitar el cap en https://ai.studio/spend o esperar reset de billing, luego UPDATE resolved_at | Manual |
| `shopping_coherence_alert_job_failures_burst` | cron `_shopping_coherence_alert_job` (P1-COH-CRON-CONSECUTIVE-FAIL · 2026-05-15) emite cuando `coh_alert_job_failures_count >= MEALFIT_COH_ALERT_CONSECUTIVE_FAIL_THRESHOLD` (default 2). Tras [P1-COH-BENIGN-SKIP · 2026-05-25] `skip_reason='below_min_plans'` NO cuenta como failure (era condición normal de baja actividad pre-launch que infló count a 14 en prod). severity=warning | mismo cron: run sin failure → INSERT/UPDATE KV count=0 + UPDATE `system_alerts SET resolved_at=NOW()` para esta alert_key | Auto (explicit, mismo cron) |
| `plan_chunk_zombie:<plan_id>` | cron `_alert_stuck_chunks` (P2-STUCK-CHUNKS · 2026-05-25) cuando hay chunks `pending+attempts=0+execute_after<NOW()-N h+created_at<NOW()-N h`. Triple-filtro incluye `execute_after<NOW()` para excluir chunks JIT-future de planes rolling multi-semana (cierre de false positive del audit P0 2026-05-25). Knob `MEALFIT_STUCK_CHUNK_OVERDUE_HOURS` default 6h. severity=warning | mismo cron: sin zombies → UPDATE resolved_at de cualquier `plan_chunk_zombie:%` previa | Auto (explicit, mismo cron) |
| `plan_stranded_partial:<plan_id>` | cron `_alert_stranded_partial_plans` (P2-STRANDED-PARTIAL · 2026-05-25) — cierre del hueco simétrico de I8. Detecta plans con `plan_data->>'generation_status'='partial' AND jsonb_array_length(days)=0 AND age > MEALFIT_STRANDED_PARTIAL_AGE_HOURS` (default 72h). I8 cubre `complete+days=[]` vía DB CHECK; este cron cubre `partial+days=[]+age>72h` (week 1 falló silente). NO añadimos CHECK porque rejectaría inserts legítimos durante generación. severity=warning | mismo cron: plan transiciona a complete/failed/cancelled → UPDATE resolved_at | Auto (explicit, mismo cron) |
| `plan_rolling_abandoned:<plan_id>` | cron `_alert_stranded_partial_plans` segunda branch (P1-ROLLING-ABANDONED · 2026-05-26) — extensión simétrica del P2-STRANDED-PARTIAL para planes rolling abandonados. Detecta plans con `generation_status='partial' AND days>0 AND age>MEALFIT_ROLLING_ABANDONED_AGE_HOURS` (default 168h, clamp [24, 720]) + `NOT EXISTS chunks IN ('pending','processing')` + `EXISTS chunk en 'pending_user_action'`. Caso real audit 2026-05-26: plan `1cb1d027` (user `8e40b0fd`) en `partial+days=3+age=62h+week2_pending_user_action`. severity=warning | mismo cron: plan transiciona a complete/failed O usuario reanuda (chunk pasa a pending/processing) → UPDATE resolved_at | Auto (explicit, mismo cron) |
| `drain_pending_facts_queue_failures_burst` | cron `_drain_pending_facts_queue` via helper SSOT `_track_cron_consecutive_failure` (P1-CRON-CONSECUTIVE-FAIL · 2026-05-26). Emite cuando count consecutivo de fallos `>= threshold` (default 2 desde `_track_cron_consecutive_failure`). "Fallo" = import db_core/process_pending_queue_sync cayó O query DISTINCT cayó O TODOS los users del batch fallaron (per-user errors aislados NO cuentan como fallo del cron global). KV key `cron_failures_count:drain_pending_facts_queue`. severity=warning | mismo cron: run sin failure → INSERT/UPDATE KV count=0 + UPDATE `system_alerts SET resolved_at=NOW()` | Auto (explicit, mismo cron) |
| `resolve_stale_scheduler_alerts_failures_burst` | cron `_resolve_stale_scheduler_alerts` via helper SSOT (P1-CRON-CONSECUTIVE-FAIL · 2026-05-26). "Fallo" = el sweep standard (primary path para alerts huérfanas `scheduler_missed_*`/`scheduler_error_*`) falló — los sweeps cascade/orphan/hard_cap son enhancements y NO cuentan como fallo del cron core. KV key `cron_failures_count:resolve_stale_scheduler_alerts`. severity=warning | mismo cron: sweep standard exitoso → reset KV + UPDATE resolved_at | Auto (explicit, mismo cron) |
| `recover_failed_chunks_for_long_plans_failures_burst` | cron `_recover_failed_chunks_for_long_plans` via helper SSOT (P1-CRON-CONSECUTIVE-FAIL · 2026-05-26). "Fallo" = toplevel `except Exception as e` capturó (batch entero abortó). KV key `cron_failures_count:recover_failed_chunks_for_long_plans`. severity=warning | mismo cron: run sin excepción toplevel → reset KV + UPDATE resolved_at | Auto (explicit, mismo cron) |
| `sweep_stale_app_kv_store_prefixes_failures_burst` | cron `_sweep_stale_app_kv_store_prefixes` (P1-KV-SWEEP · 2026-05-26) via helper SSOT (P1-CRON-CONSECUTIVE-FAIL). "Fallo" = TODOS los prefixes del catálogo `_KV_SWEEP_PREFIXES` fallaron (algún prefix OK significa que la conexión DB sirve; fallo total apunta a problema sistémico). KV key `cron_failures_count:sweep_stale_app_kv_store_prefixes`. severity=warning | mismo cron: cualquier prefix OK → reset KV + UPDATE resolved_at | Auto (explicit, mismo cron) |
| `sweep_stale_scheduler_missed_alerts_failures_burst` | cron `_sweep_stale_scheduler_missed_alerts` (P1-ORPHAN-MISSED-SWEEP · 2026-05-27) via helper SSOT (P1-CRON-CONSECUTIVE-FAIL). "Fallo" = toplevel `except Exception` capturó (query falló o `cascade_active` guard query falló). `cascade_active=True` NO cuenta como fallo (es comportamiento diseñado: preservar señal sistémica). KV key `cron_failures_count:sweep_stale_scheduler_missed_alerts`. severity=warning | mismo cron: run sin excepción toplevel → reset KV + UPDATE resolved_at | Auto (explicit, mismo cron) |
| `nightly_refresh_all_pending_snapshots_failures_burst` | cron `_nightly_refresh_all_pending_snapshots` (P2-NIGHTLY-OBS · 2026-05-26) via helper SSOT (P1-CRON-CONSECUTIVE-FAIL). "Fallo" = toplevel `except Exception` capturó (algo sistémico — query falló, import falló, etc.). Per-user errors aislados o budget-aborts NO cuentan como fallo del cron global. KV key `cron_failures_count:nightly_refresh_all_pending_snapshots`. severity=warning | mismo cron: run sin excepción toplevel → reset KV + UPDATE resolved_at | Auto (explicit, mismo cron) |
| `plan_persist_failed:<user_id>` | `_persist_plan_persist_failed_alert` (services.py, P2-PLAN-PERSIST-FAILED · 2026-05-30) cuando la persistencia de un plan ya generado falla: (a) `_postprocess_pipeline_result` chunking-path detecta `save_partial_plan_get_id() -> None` (INSERT meal_plans fallido: pool exhaustion, statement_timeout, CHECK I8, serialization) → propaga `result['_persist_failed']` a los 3 consumidores (sync 503 / SSE error event / done-callback KV failed) en vez de un phantom `complete`; (b) `_save_plan_and_track_background` except (no-chunking save post-respuesta falla). severity=critical | Manual: investigar logs del path de persistencia para el usuario afectado; verificar si el plan existe en `meal_plans`; UPDATE resolved_at tras confirmar (incidente transient de pool/timeout → resolver; recurrente → escalar) | Manual |
| `bg_task_timeout:<task_name>` | `_persist_bg_task_timeout_alert` (bg_executor.py) cuando el watcher daemon de un `submit_bg_task` detecta `FutureTimeout` (task excedió `MEALFIT_BG_TASK_TIMEOUT_S`, default 120s — patrón habitual: LLM/Supabase cuelga upstream). severity=warning. [P1-PROD-AUDIT-2 · 2026-05-30] Pre-fix NO tenía resolver → rojo-permanente latente + blind-spot del test (`bg_executor.py` no estaba en `_EMITTER_FILES`) | [P1-PROD-AUDIT-2] sweep window-based en `_resolve_stale_scheduler_alerts`: UPDATE resolved_at cuando `triggered_at` excede `MEALFIT_BG_TASK_TIMEOUT_ALERT_TTL_H` (default 2h) sin re-disparo (`AND resolved_at IS NULL`) | Auto (explicit) |
| `rate_limiter_bucket_saturation` | `_emit_rl_saturation_alert` (rate_limiter.py, P2-RATELIMITER-BUCKET-METRICS) en el cleanup tick cuando `_bucket_count > MEALFIT_RATE_LIMITER_BUCKET_LIMIT_WARN` (default 100k — posible memory leak / botnet de IPs). severity=warning. [P1-PROD-AUDIT-2 · 2026-05-30] Pre-fix NO tenía resolver → rojo-permanente latente + blind-spot del test (`rate_limiter.py` no estaba en `_EMITTER_FILES`) | [P1-PROD-AUDIT-2] `_resolve_rl_saturation_alert` en el mismo cleanup tick cuando `_bucket_count <= threshold` (cardinalidad volvió bajo umbral): UPDATE resolved_at (`AND resolved_at IS NULL`) | Auto (explicit) |

## Cómo añadir un nuevo `alert_key`

(1) decidir modelo de resolution; (2) añadir fila a la tabla de arriba con productor/resolver/modelo (el test `test_p2_audit_4_alert_keys_documented.py` falla en CI si emisor sin row o ghost row); (3) si Auto (explicit), añadir `UPDATE system_alerts SET resolved_at = NOW() WHERE alert_key = '...' AND resolved_at IS NULL` al callsite que resuelve; (4) test de regresión idempotencia del resolver. SOPs detallados (alerts Manual + limpieza one-shot de huérfanas): runbook arriba.
